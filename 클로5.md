# 클로5 — /events(향후 7일 문화 이벤트) 최종 합의안

> 작성: Claude. 지피5.md를 실제 코드와 전수 대조한 초정밀 검증 결과를 반영한 **최종 합의·구현 계약 문서**.
> 구현자는 **지피5.md를 본문으로 삼되, 본 문서의 [S1]~[S8] 수정 조항이 지피5와 충돌하면 본 문서를 우선**한다.

---

## 1. 합의 판정

**합의한다. 지피5.md 기준으로 구현한다.** 단, 아래 검증에서 발견된 보완 조항 8건([S1]~[S8])을 포함한다.

## 2. 코드 대조 검증 결과 (지피5의 주장 사실확인)

| 지피5의 주장 | 검증 결과 |
|---|---|
| `requireEnv()` 로 키를 읽는다 | ✅ 실존 — `lib/env.ts:7` `export function requireEnv(key)` |
| `date-fns` 사용 가능 | ✅ 이미 의존성 — `package.json` `"date-fns": "^4.1.0"` (신규 설치 불필요) |
| `npm run pipeline -- "$SLUG"` 호출 가능 | ✅ 실존 — `package.json` `"pipeline": "tsx scripts/pipeline/run.ts"` |
| `lib/paths.ts`에 이벤트 경로 헬퍼 추가 | ✅ 패턴 일치 — `ROOT`/`CONTENT_DIR`/`issueDir`/`issueJsonPath` 구조 확인, 동일 형식으로 `EVENTS_DIR`/`eventDir`/`eventJsonPath` 추가 |
| `publishToGit(slug)`가 `content/issues/{slug}`만 add | ✅ 사실 — `scripts/pipeline/08-publish-send.ts` 확인. 이벤트 커밋 누락 지적 타당 |
| make.sh에서 이벤트 생성을 run.ts(내부 next build)보다 먼저 | ✅ 타당 — 단 이 빌드는 **로컬 검수용**이다. publish.sh(PUBLISH_ONLY)는 로컬 빌드를 하지 않으며, **배포 포함 여부는 Vercel 재빌드가 읽는 커밋([S2])이 결정**한다 |
| TourAPI `searchFestival2` 200/0000 호출 확인 | ⚠️ **레포 코드로는 검증 불가**(지피5 실측 주장 수용하되, 첫 실행 때 raw 응답 덤프로 재확인 [S8]) |
| `koreaAPI.md`는 공개 저장소에 올리면 안 된다 | ⚠️ gitignore 라인 부재 발견 → **라인 추가됨(워킹트리 — 커밋 필요)**([S1]). 단 git 추적/이력에 한 번도 없어 **유출은 없었음** |

## 3. 수정·보완 조항 (최종 합의에 포함, 지피5에 우선)

### [S1] 보안 — koreaAPI.md 보호
- `.gitignore`에 `koreaAPI.md` 라인 **추가됨(현재 워킹트리 변경 상태 — 다음 커밋에 반드시 포함해야 보호가 확정됨)**. 키 파일은 추적·커밋 절대 금지. (참고: koreaAPI.md는 git 이력에 한 번도 올라간 적 없음 — 현재까지 유출 없음)
- 커밋 전 비밀 스캔 목록에 `koreaAPI`를 포함한다(기존 NaverAPI·Brevo 등과 동일 취급).
- 키는 `.env.local`의 `TOUR_API_SERVICE_KEY`로만 사용, `requireEnv("TOUR_API_SERVICE_KEY")`로 읽는다.
- **Vercel 환경변수에는 추가 불필요** — 이벤트 수집은 로컬 make 단계에서만 실행되고, Vercel은 커밋된 정적 `events.json`을 렌더만 한다.

### [S2] publishToGit — `content/events/{slug}` 는 **존재할 때만** add
지피5대로 무조건 `git add content/events/{slug}` 하면, 이벤트가 생성되지 않은 발행(장애·예외 시)에서 git이 `pathspec did not match`로 **throw → 발행 전체 실패**한다.
```ts
// 08-publish-send.ts publishToGit 내 (fs는 이미 import되어 있음 — path는 import 안 돼 있으므로 문자열 경로 사용)
execFileSync("git", ["add", `content/issues/${slug}`], ...);
if (fs.existsSync(`content/events/${slug}`)) {
  execFileSync("git", ["add", `content/events/${slug}`], ...);
}
```
슬러그 한정 add 원칙(테스트호 오발행 방지)은 이벤트에도 동일 적용한다.

### [S3] 이벤트 생성 실패 = 주간 발행 중단 금지 → **빈 문서 폴백**
지피5는 "이벤트 실패 시 전체 제작 실패"(+`ALLOW_MISSING_EVENTS=1` 예외)다. 최종 합의는 반대로 한다:
- **기본 동작**: `run-events.ts`가 수집/분석 실패 시 **빈 `events.json`(events: [], meta.note에 실패 사유) 을 생성**하고 큰 경고 출력 후 **뉴스 제작은 계속**한다.
- 이유 ①: TourAPI/네이버 일시 장애가 **핵심 서비스(주간 뉴스 발행)를 막으면 안 된다**(지피1 원래 원칙: "발행 실패 지점을 늘리지 않는다").
- 이유 ②: 빈 문서를 새로 굽지 않으면 **지난주 events.json이 그대로 재빌드**되어, "향후 7일"이라며 **이미 지난 이벤트를 노출**하는 더 나쁜 상태가 된다. 빈 문서 + "이번 주 이벤트 정보를 불러오지 못했습니다" 빈 상태 화면이 정직하다.
- `publish.sh`의 존재 검증은 "없으면 **경고**"로 충분. 단 "항상 존재"는 **make.sh로 새로 제작한 슬러그에 한해서**다 — 이벤트 도입 전 구 발행본(2026-05-25·2026-05-31)을 재발행하면 events.json이 없으므로, 이때는 경고만 뜨고 발행이 계속되는 것이 올바른 동작이다. `ALLOW_MISSING_EVENTS` 환경변수는 **도입하지 않는다**(불필요해짐).

### [S4] 배포 폴링에 /events 추가하지 않는다
`/issues/{slug}`와 `/events`는 **같은 배포 산출물**이다. `pollDeploy`가 `/issues/{slug}` 200을 확인하면 `/events`도 같은 빌드로 이미 떠 있다. 폴링 추가는 결합만 늘리므로 생략(지피5 해당 항목 삭제).

### [S5] 검수 항목 1개 추가
지피5 검수 목록에 다음을 추가한다: **"이미 시작했지만 기간 중인 이벤트(장기 전시·축제)가 포함되는가"** — 90일 소급 수집의 핵심 목적이므로 첫 실행에서 반드시 육안 확인.

### [S6] make.sh 수정은 최소 diff로
기존 make.sh는 `LLM_PROVIDER=... npx tsx scripts/pipeline/run.ts "$SLUG"`를 직접 호출한다. 이 줄은 **그대로 두고**, 그 앞에 한 줄만 추가한다:
```bash
npx tsx scripts/pipeline/events/run-events.ts "$SLUG" || echo "⚠️ 이벤트 생성 실패 — 뉴스 제작은 계속(빈 events.json 폴백)"
```
(`npm run pipeline` 치환은 불필요한 변경 — LLM_PROVIDER 전달 방식이 깨질 수 있음.)

### [S7] 날짜 유틸 확정
- KST 안전 계산은 **이미 의존성에 있는 `date-fns`** 사용: `parseISO(slug)` + `addDays`/`subDays` + `format(d, "yyyyMMdd")`(로컬 기준 — 실행 머신이 KST인 이 PC/Mac 환경 전제. UTC 머신에서 돌릴 일이 생기면 `TZ=Asia/Seoul` 지정).
- `new Date(slug).toISOString()` 금지·기본 slug의 UTC slice 금지(지피5와 동일 — 채택).
- **기본 slug(인자 생략 시) 구체 코드 확정**: `format(new Date(), "yyyy-MM-dd")`(date-fns, 로컬=KST). ⚠️ 기존 run.ts(24행)·08-publish-send(112행)는 `new Date().toISOString().slice(0,10)`(UTC)를 쓰고 있어 자정 무렵 날짜가 어긋날 수 있음 — 신규 코드는 KST 방식을 따르고, 기존 코드와의 불일치를 주석으로 명시한다.
- 뉴스 상대 날짜("오는 8일"·"내일"·"이번 주") 해석은 1차에서 하지 않는다 — **명시적 절대 날짜가 추출된 뉴스 후보만 노출**(지피5와 동일 — 채택. 상대 표현은 기사 pubDate 기준 해석이 필요해 오류 위험이 크다).

### [S8] TourAPI 호출 안전화 — `eventEndDate` 파라미터를 보내지 않는다
- `eventEndDate`의 필터 의미론(행사 **시작일** 상한인지 **종료일** 상한인지)은 **미검증**이다. 만약 "종료일 ≤ 파라미터" 필터라면, `eventEndDate=slug+7`은 **slug+7 이후에 끝나는 장기 전시·축제를 API 단계에서 통째로 제외** — 90일 소급 수집의 목적 자체가 무너진다(로컬 필터로는 못 받은 데이터를 복구할 수 없음).
- 따라서 **요청에는 `eventStartDate=slug−90`만 보내고, 상한(`startDate <= slug+7`)은 전부 로컬에서 필터**한다. 수집량이 다소 늘지만(최대 20페이지 상한 내) 의미론 리스크가 0이 된다.
- **첫 실행 검수 필수**: raw 응답 1건을 덤프해 ① `resultCode`/`items` 구조(단일 객체 vs 배열) ② `serviceKey` 인코딩 통과(이중 인코딩 시 `SERVICE_KEY_IS_NOT_REGISTERED` 류 오류) ③ 진행 중 장기 이벤트가 실제로 응답에 포함되는지 육안 확인.

## 4. 지피5에서 그대로 채택하는 핵심 (변경 없음)

- 데이터 소스: **TourAPI `KorService2/searchFestival2`(공식 일정·장소·이미지) + 네이버 뉴스(대형 이벤트 발굴·관심도)**, 최종 정렬은 출처가 아니라 **점수**(BTS 공연이 1위 가능).
- 수집 넓게(`slug−90일`~`slug+7일`) → 로컬 필터(`startDate <= to && endDate >= from`), `endDate` 없으면 `startDate`로 보정.
- 페이지네이션: `totalCount` 기준 + 최대 20페이지 강제 종료, `items.item` 단일 객체/배열 정규화 필수.
- 점수 모델: importance .30 / interest .25 / urgency .15 / scale .15 / reliability .15 (0–100, v1 임시값 주석).
- 날짜 불명 뉴스 후보는 **노출 금지**, `dateConfidence`는 내부·근거 설명용.
- 타입(`types/event.ts`)·로더(`lib/events.ts` 분리, `lib/content.ts` 불가침)·`lib/tour-api.ts`(15초 AbortController, 401/resultCode 처리)·상위 20개 저장·`/events` force-static·빈 데이터에도 빌드 성공·홈 상위 3개 섹션(없으면 숨김)·헤더 "이번 주 이벤트" 링크·이미지 없을 때 텍스트 카드·`kct-card`/`on-navy`/`text-accent` 재사용.
- package.json: `events`/`events:collect`/`events:analyze` 스크립트.
- `/detailCommon2` 보강은 선택(상위 ~20개 한정, 1차 생략 가능).
- 화면 문구: "일정은 주최 측 사정에 따라 변경될 수 있으니 방문 전 공식 링크를 확인하세요" 필수.

## 5. 최종 구현 순서 (지피5 순서 + S 조항 병합)

1. `.gitignore`의 koreaAPI.md 라인을 **커밋에 포함**([S1] — 라인은 추가돼 있음)
2. `types/event.ts`
3. `lib/paths.ts`에 `EVENTS_DIR`/`eventDir`/`eventJsonPath`
4. `lib/tour-api.ts` (requireEnv, 페이지네이션, 단일/배열 정규화, 15s timeout, **eventEndDate 미전송**[S8])
5. `lib/events.ts` (allEventSlugs/readEvents/latestEvents — content/events 부재 시 빈 결과)
6. `scripts/pipeline/events/01-collect-events.ts` (`import "@/lib/load-env"` 포함, raw-events.json)
7. `scripts/pipeline/events/02-analyze-events.ts` (병합·점수·상위20 → events.json)
8. `scripts/pipeline/events/run-events.ts` (**실패 시 빈 events.json 폴백** [S3], KST 기본 slug [S7])
9. `package.json` 스크립트 3종
10. `components/EventView.tsx` + `app/events/page.tsx` (force-static, 빈 상태 화면)
11. `components/SiteHeader.tsx` 링크 + `app/page.tsx` 최신 이벤트 3개 섹션
12. `scripts/make.sh` 한 줄 추가([S6])
13. `scripts/publish.sh` — `content/events/{slug}` **부재 시 경고만 출력하고 발행은 계속**([S3]) + `08-publish-send.ts` publishToGit **조건부** events add([S2])
14. 검증: `npm run events -- {slug}` → `npx tsc --noEmit` → `npm run build` → `git diff --check`

## 6. 최종 검수 체크리스트

- [ ] `content/events/{slug}/events.json` 생성, `range` 정확(KST)
- [ ] 종료 이벤트 미포함 / 7일 밖 미포함 / **진행 중(이미 시작) 장기 이벤트 포함**([S5])
- [ ] 날짜 없는 뉴스 후보 미노출 / 점수 내림차순
- [ ] TourAPI 공식 행사 유입 + 뉴스 기반 대형 이벤트 유입(날짜 확인된 것)
- [ ] `/events` 빌드 성공(데이터 없음=빈 상태 화면) / 홈 상위 3개 / 헤더 링크
- [ ] **`content/events/` 디렉터리 자체가 없는 상태에서 `npm run build` 성공**(첫 배포 시뮬레이션 — `lib/events.ts`의 `fs.existsSync` 가드 검증)
- [ ] **TourAPI 첫 호출 raw 응답 1건 덤프 확인**: resultCode·items 구조·키 인코딩·진행 중 장기 이벤트 포함 여부([S8])
- [ ] 이벤트 생성 실패 시뮬레이션: 빈 events.json 폴백 + 뉴스 제작 계속([S3])
- [ ] **신규 제작 슬러그 발행**: events.json(빈 문서라도) 커밋에 포함되는가([S2]+[S3])
- [ ] **구 발행본(2026-05-31 등) 재발행**: events 없음 경고만 뜨고 발행이 죽지 않는가([S2])
- [ ] `git status`에 koreaAPI.md 미노출(ignore 확인) + .gitignore 변경 커밋됨([S1])

## 7. 한 줄 결론

**지피5 채택 + [S1]~[S8] 보완으로 최종 합의.** 가장 중요한 네 가지: ① 키 파일 gitignore 라인 추가 — **다음 커밋에 반드시 포함**, ② 이벤트 장애가 주간 뉴스 발행을 절대 막지 않으며(빈 문서 폴백), ③ 발행 커밋은 존재 검사 후 슬러그 한정으로만 넓히고, ④ TourAPI는 `eventEndDate`를 보내지 않고 상한을 로컬에서 필터한다(의미론 리스크 제거).
