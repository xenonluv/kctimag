# 향후 7일 문화 이벤트 큐레이션 최종 구현안

## 결론

구현한다.

현재 프로젝트는 지난 7일 문화 뉴스를 큐레이션한다. 여기에 `/events` 페이지를 추가해 **앞으로 7일 안에 실제로 열릴 문화 이벤트**를 중요도, 관심도, 임박성, 공식성 기준으로 정렬해 보여준다.

핵심은 지역축제 목록 페이지가 아니다. BTS 공연, 대형 K-pop 콘서트, 영화제, 국립 박물관 특별전, 대형 전시, 공연, e스포츠 결승, 팝업스토어, 지역 대표 축제처럼 사람들이 실제로 관심을 가질 만한 이벤트가 위로 올라와야 한다.

## 중요한 전제

TourAPI 키는 사용 가능하다.

`/mnt/c/Users/User/Documents/kguide.pdf`는 TourAPI 상세 매뉴얼이 아니라 공공데이터포털 Swagger 호출 가이드다. 문서상 핵심은 `serviceKey는 일반 인증키(Decoding)을 입력`하라는 내용이다.

`koreaAPI.md`의 일반 인증키를 `serviceKey`로 넣어 아래 엔드포인트를 호출했을 때 정상 응답을 확인했다.

```text
GET https://apis.data.go.kr/B551011/KorService2/searchFestival2
HTTP 200
resultCode: 0000
resultMsg: OK
```

문서에는 인증키 원문을 절대 넣지 않는다. 구현에서는 환경변수만 사용한다.

```text
TOUR_API_SERVICE_KEY=공공데이터포털 일반 인증키
```

## 최종 기능 범위

추가할 기능:

- `/events` 페이지 추가
- 오늘 기준 향후 7일 이벤트 표시
- 한국관광공사 TourAPI `KorService2/searchFestival2`로 공식 축제/행사 수집
- 기존 네이버 뉴스 API로 대형 문화 이벤트 후보 수집
- 두 소스를 병합하고 중복 제거
- 중요도, 관심도, 규모, 임박성, 공식성 점수 계산
- 점수순 정렬
- `content/events/{slug}/events.json` 정적 데이터 저장
- 홈페이지와 헤더에서 `/events` 진입 링크 제공

1차 구현에서 제외할 것:

- 티켓 예매처 전체 크롤링
- 인스타그램, 유튜브, X 같은 SNS 수집
- 개인 위치 기반 추천
- 티켓 가격/잔여석 실시간 추적
- 유료 API 추가 연동
- LLM 필수 의존

## 데이터 소스 우선순위

### 1. TourAPI 공식 행사 데이터

공식 출처다. `/searchFestival2`를 사용한다.

호출 형식:

```text
https://apis.data.go.kr/B551011/KorService2/searchFestival2
  ?serviceKey=...
  &MobileOS=ETC
  &MobileApp=kctimag
  &_type=json
  &numOfRows=100
  &pageNo=1
  &eventStartDate=YYYYMMDD
  &eventEndDate=YYYYMMDD
```

필수 파라미터:

- `serviceKey`: `TOUR_API_SERVICE_KEY`
- `MobileOS`: `ETC`
- `MobileApp`: `kctimag`
- `_type`: `json`
- `eventStartDate`: 기준일보다 앞선 보수적 시작일, `YYYYMMDD`
- `eventEndDate`: 기준일 + 7일, `YYYYMMDD`
- `numOfRows`: 100
- `pageNo`: 1부터 반복

중요:

- `searchFestival2`의 날짜 조건은 "앞으로 시작할 행사"만 안전하게 잡는다고 가정하면 안 된다.
- 이미 시작했지만 이번 주에도 진행 중인 전시/공연/축제까지 포함하려면 수집은 넓게 하고, 분석 단계에서 직접 필터링한다.
- 권장 수집 범위는 `eventStartDate = 기준일 - 90일`, `eventEndDate = 기준일 + 7일`이다.
- 최종 노출 조건은 `startDate <= 기준일 + 7일` 그리고 `endDate >= 기준일`이다.
- `endDate`가 없으면 `startDate`와 같은 날 종료되는 이벤트로 본다.

응답에서 사용할 필드:

- `contentid`
- `title`
- `addr1`
- `addr2`
- `eventstartdate`
- `eventenddate`
- `firstimage`
- `firstimage2`
- `mapx`
- `mapy`
- `tel`
- `cat1`, `cat2`, `cat3`
- `modifiedtime`

TourAPI 응답은 지역축제와 관광 행사에 강하지만, 대형 콘서트와 팬미팅 같은 엔터테인먼트 이벤트는 누락될 수 있다. 그래서 네이버 뉴스 후보 수집이 반드시 필요하다.

선택 보강:

- 상위 후보에 한해 `/detailCommon2`를 추가 호출해 개요, 홈페이지 URL, 상세 이미지를 보강할 수 있다.
- 1차 구현에서는 `searchFestival2`만으로도 가능하다.
- `/detailCommon2`를 호출할 경우 일일 트래픽 1000건 제한을 고려해 최종 후보 20개 안팎으로 제한한다.

### 2. 네이버 뉴스 기반 대형 이벤트 후보

기존 `lib/naver.ts`의 `searchNews()`를 재사용한다.

검색 쿼리는 다음 그룹으로 나눈다.

```ts
const EVENT_NEWS_QUERIES = [
  "K팝 콘서트 다음주",
  "아이돌 콘서트 예정",
  "BTS 공연 예정",
  "블랙핑크 콘서트 예정",
  "세븐틴 콘서트 예정",
  "스트레이 키즈 콘서트 예정",
  "팬미팅 개최",
  "월드투어 서울",
  "영화제 개막",
  "국제영화제 개막",
  "전시 개막",
  "미술관 특별전",
  "박물관 특별전",
  "문화행사 개최",
  "팝업스토어 오픈",
  "e스포츠 결승",
  "게임쇼 개최",
  "뮤지컬 개막",
  "공연 개막"
];
```

네이버 뉴스는 이벤트 존재를 바로 보증하지 않는다. 기사에서 정확한 날짜가 추출되지 않으면 수집 후보로만 두고, 최종 `events.json` 노출에서는 제외한다. `dateConfidence`는 표시 여부가 아니라 내부 검수와 근거 설명용으로만 사용한다.

## 파일 추가/수정 목록

### 새 파일

```text
types/event.ts
lib/events.ts
lib/tour-api.ts
scripts/pipeline/events/01-collect-events.ts
scripts/pipeline/events/02-analyze-events.ts
scripts/pipeline/events/run-events.ts
app/events/page.tsx
components/EventView.tsx
```

### 수정 파일

```text
package.json
lib/paths.ts
components/SiteHeader.tsx
app/page.tsx
scripts/make.sh
scripts/publish.sh
scripts/pipeline/08-publish-send.ts
```

`lib/content.ts`는 수정하지 않는다. 이벤트 데이터 로더는 `lib/events.ts`로 분리한다.

`lib/paths.ts`에는 이벤트 JSON 경로 헬퍼를 추가한다.

`scripts/make.sh`와 `scripts/publish.sh`는 이벤트 페이지를 실제 운영 발행물에 포함하려면 수정해야 한다. 기존 주간 뉴스만 만들 때는 필요 없었지만, `/events`까지 정적 데이터로 같이 갱신하려면 make 단계에서 이벤트 JSON 생성이 들어가야 한다.

`scripts/pipeline/08-publish-send.ts`도 수정해야 한다. 현재 `publishToGit(slug)`는 `content/issues/{slug}`만 `git add`하므로, 이벤트 JSON을 생성해도 `content/events/{slug}`가 커밋되지 않는다. 이 상태로 합의하면 `/events` 데이터가 배포에 빠진다.

## 타입 설계

`types/event.ts`를 추가한다.

```ts
export type EventSourceType = "tour-api" | "news";

export type EventCategory =
  | "concert"
  | "festival"
  | "exhibition"
  | "performance"
  | "film"
  | "esports"
  | "popup"
  | "heritage"
  | "other";

export interface EventSourceRef {
  type: EventSourceType;
  title: string;
  url?: string;
  outlet?: string;
  pubDate?: string;
  contentId?: string;
}

export interface EventScores {
  importance: number;
  interest: number;
  urgency: number;
  scale: number;
  reliability: number;
}

export interface CultureEvent {
  id: string;
  title: string;
  category: EventCategory;
  region?: string;
  venue?: string;
  address?: string;
  startDate: string;
  endDate?: string;
  imageUrl?: string;
  officialUrl?: string;
  description?: string;
  coordinates?: {
    lat: number;
    lng: number;
  };
  sources: EventSourceRef[];
  scores: EventScores;
  score: number;
  rationale: string;
  dateConfidence: "high" | "medium" | "low";
}

export interface EventsDoc {
  slug: string;
  generatedAt: string;
  range: {
    from: string;
    to: string;
  };
  meta: {
    title: string;
    dek: string;
    totalCandidates: number;
    selected: number;
    sourceBreakdown: {
      tourApi: number;
      news: number;
    };
    note: string;
  };
  events: CultureEvent[];
}
```

## 환경변수

`.env.local` 또는 운영 환경에 추가한다.

```text
TOUR_API_SERVICE_KEY=일반 인증키
```

주의:

- 키 원문을 `지피5.md`, 코드, Git 커밋에 넣지 않는다.
- `koreaAPI.md`에는 현재 키가 들어 있으므로 공개 저장소에 올리면 안 된다.
- TourAPI 호출은 `requireEnv("TOUR_API_SERVICE_KEY")`로 읽는다.

## TourAPI 클라이언트

`lib/tour-api.ts`를 추가한다.

역할:

- 환경변수에서 키 읽기
- `searchFestival2` 호출
- 페이지네이션 처리
- 공공데이터포털 오류 응답 처리
- JSON 구조가 배열/단일 객체일 때 모두 대응

중요 구현 포인트:

```ts
const ENDPOINT = "https://apis.data.go.kr/B551011/KorService2/searchFestival2";
```

`serviceKey`는 `URLSearchParams`에 그대로 넣는다. 현재 키는 일반 인증키이며 실제 호출 성공을 확인했다.

오류 처리:

- HTTP 401: 인증키 미활성/오입력으로 중단
- `resultCode !== "0000"`: 공공데이터 응답 오류로 중단
- items 없음: 빈 배열 반환
- fetch timeout은 `AbortController`로 15초 제한

응답 아이템 타입 예:

```ts
interface TourFestivalItem {
  contentid: string;
  title: string;
  addr1?: string;
  addr2?: string;
  eventstartdate: string;
  eventenddate?: string;
  firstimage?: string;
  firstimage2?: string;
  mapx?: string;
  mapy?: string;
  tel?: string;
  cat1?: string;
  cat2?: string;
  cat3?: string;
}
```

## 이벤트 데이터 로더

`lib/events.ts`를 추가한다.

역할:

- `content/events` 하위 slug 목록 조회
- 특정 slug의 `events.json` 읽기
- 최신 이벤트 문서 반환

기존 `lib/content.ts`의 패턴과 유사하게 만든다.

```ts
export function allEventSlugs(): string[]
export function readEvents(slug: string): EventsDoc | null
export function latestEvents(): EventsDoc | null
```

`lib/content.ts`에 무리하게 섞지 말고 `lib/events.ts`로 분리하는 것을 권장한다. 다만 홈페이지에서 최신 이벤트를 같이 보여줘야 하므로 `app/page.tsx`에서 `latestEvents()`를 import한다.

`lib/paths.ts`에는 다음 헬퍼를 추가한다.

```ts
export const EVENTS_DIR = path.join(ROOT, "content", "events");

export function eventDir(slug: string): string {
  return path.join(EVENTS_DIR, slug);
}

export function eventJsonPath(slug: string): string {
  return path.join(eventDir(slug), "events.json");
}
```

## 수집 스크립트

`scripts/pipeline/events/01-collect-events.ts`를 추가한다.

이벤트 CLI 진입점에는 기존 파이프라인과 동일하게 환경변수를 로드한다.

```ts
import "@/lib/load-env";
```

적용 대상:

- `scripts/pipeline/events/run-events.ts`
- `scripts/pipeline/events/01-collect-events.ts`

수집 결과 파일:

```text
.pipeline-tmp/{slug}/raw-events.json
```

수집 범위:

- 기준일: 인자로 받은 slug 날짜
- `from`: slug
- `to`: slug + 7일
- 의미: 기준일 현재부터 7일 뒤 날짜까지 시작하거나 진행 중인 이벤트를 포함한다.
- TourAPI 호출용 `collectFrom`: slug - 90일
- 최종 필터용 `displayFrom`: slug
- 최종 필터용 `displayTo`: slug + 7일

TourAPI:

- `searchFestival2`로 행사 수집
- `eventStartDate=collectFrom`
- `eventEndDate=displayTo`
- `numOfRows=100`
- `pageNo` 반복
- 분석 단계에서 `startDate <= displayTo && endDate >= displayFrom` 조건으로 다시 필터링

페이지네이션:

- 응답의 `body.totalCount`, `body.numOfRows`, `body.pageNo`를 숫자로 변환한다.
- `pageNo * numOfRows >= totalCount`이면 종료한다.
- `totalCount`가 없거나 비정상인 경우 `items.length < numOfRows`이면 종료한다.
- 무한 루프 방지를 위해 최대 20페이지에서 강제 종료한다.

날짜 계산 주의:

- `slug`는 `YYYY-MM-DD` 문자열이다.
- 한국 서비스 기준이므로 날짜 범위 계산에서 UTC 변환으로 하루가 밀리면 안 된다.
- `new Date(slug).toISOString()`로 `YYYYMMDD`를 만들지 않는다.
- `date-fns`의 `parseISO`, `addDays`, `subDays`, `format`을 쓰거나, 로컬 날짜 문자열을 안전하게 계산하는 유틸을 만든다.
- TourAPI 요청용 날짜는 최종적으로 `YYYYMMDD` 문자열이어야 한다.

네이버 뉴스:

- `EVENT_NEWS_QUERIES`를 순회
- query당 `display=20`, `sort=date`
- 중복 URL 제거
- 제목/설명에서 이벤트성 키워드가 없으면 제외
- 정확한 날짜 단서가 없는 뉴스는 수집 후보에는 남기되 분석 단계에서 최종 노출 제외

수집 데이터 타입:

```ts
interface RawEventCandidate {
  sourceType: "tour-api" | "news";
  title: string;
  description?: string;
  url?: string;
  outlet?: string;
  pubDate?: string;
  contentId?: string;
  address?: string;
  region?: string;
  startDate?: string;
  endDate?: string;
  imageUrl?: string;
  lat?: number;
  lng?: number;
  raw: unknown;
}
```

## 분석 스크립트

`scripts/pipeline/events/02-analyze-events.ts`를 추가한다.

입력:

```text
.pipeline-tmp/{slug}/raw-events.json
```

출력:

```text
content/events/{slug}/events.json
```

분석 절차:

1. TourAPI 후보와 뉴스 후보를 하나의 배열로 합친다.
2. 제목 정규화로 중복을 병합한다.
3. 같은 날짜와 비슷한 제목이면 같은 이벤트로 본다.
4. 시작일이 없으면 제외한다.
5. 뉴스 후보는 제목/설명에서 날짜가 명확히 추출되지 않으면 제외한다.
6. 최종 기간 조건을 적용한다: `startDate <= displayTo && endDate >= displayFrom`.
7. 점수를 계산한다.
8. 상위 20개만 저장한다.

날짜 처리:

- TourAPI 날짜는 `YYYYMMDD`를 `YYYY-MM-DD`로 변환한다.
- `endDate`가 없으면 `startDate`와 같은 값으로 보정한다.
- 뉴스 날짜 추출은 1차에서 보수적으로 한다.
- `오늘`, `내일`, `이번 주`, `다음 주` 같은 상대 날짜만 있고 정확한 날짜가 없으면 표시 대상에서 제외한다.
- 잘못된 미래 예측을 막기 위해 정확한 날짜가 없는 뉴스 이벤트는 `events.json`에 넣지 않는다.
- `run-events.ts`에서 slug 인자가 없을 때도 UTC 기준 `new Date().toISOString().slice(0, 10)`를 쓰지 않는다.
- 기본 slug는 한국 시간 기준 `YYYY-MM-DD`로 계산한다.

## 점수 모델

총점은 100점 만점으로 계산한다.

```text
score =
  importance * 0.30
  + interest * 0.25
  + urgency * 0.15
  + scale * 0.15
  + reliability * 0.15
```

각 항목은 0-100점이다.

### importance

이벤트 자체의 문화적 중요도.

가산:

- 글로벌 K-pop 아티스트 공연
- 국제 영화제/비엔날레/아트페어
- 국립 박물관/미술관 특별전
- 대형 게임/e스포츠 결승
- 지역 대표 축제

### interest

사람들이 얼마나 관심 가질 가능성이 높은지.

가산:

- 네이버 뉴스 중복 언급 수
- 여러 언론사 보도
- BTS, 블랙핑크, 뉴진스, 세븐틴, 스트레이 키즈 등 대형 키워드
- 티켓, 예매, 매진, 개막, 첫 공개 같은 관심 신호

### urgency

임박성.

가산:

- 오늘/내일 시작: 높음
- 3일 이내 시작: 중상
- 7일 이내 시작: 보통

이미 종료된 이벤트는 제외한다. 진행 중인 이벤트는 남긴다.

### scale

규모.

가산:

- 전국/국제/월드투어/아시아투어/대형 페스티벌
- 서울, 부산, 인천, 대구, 광주 등 광역 단위 대형 행사
- 국립기관, 대형 공연장, 대형 브랜드

### reliability

출처 신뢰도.

가산:

- TourAPI 공식 행사: 높음
- 뉴스 여러 건이 같은 이벤트를 보도: 높음
- 뉴스 1건뿐이고 날짜/장소 불명확: 낮음

## 카테고리 분류

키워드 기반으로 1차 분류한다.

```text
concert: 콘서트, 공연, 팬미팅, 월드투어, K팝, 아이돌
festival: 축제, 페스티벌, 문화제, 야행
exhibition: 전시, 특별전, 미술관, 박물관, 아트페어, 비엔날레
performance: 뮤지컬, 연극, 무용, 클래식, 오페라
film: 영화제, 시사회, 상영회
esports: e스포츠, 게임대회, 결승, LCK, 게임쇼
popup: 팝업, 팝업스토어, 브랜드 행사
heritage: 국가유산, 궁궐, 문화유산, 전통
other: 기타
```

## 네이버 뉴스 이벤트 후보 필터

포함 키워드:

```text
콘서트, 공연, 팬미팅, 월드투어, 축제, 페스티벌, 영화제, 개막,
전시, 특별전, 미술관, 박물관, 뮤지컬, 연극, 오페라, 클래식,
팝업, 팝업스토어, e스포츠, 게임쇼, 결승, 개최, 오픈
```

제외 키워드:

```text
정치, 선거, 국회, 대통령, 장관, 외교, 주가, 증시, 부동산,
사건, 사고, 범죄, 소송, 논란, 사망, 재판
```

주의:

- 제목만 보지 말고 제목 + 설명 전체를 검사한다.
- 정치/경제 기사에 문화 단어가 들어간 경우를 막기 위해 제외 키워드를 먼저 적용한다.
- 뉴스만으로 날짜가 불명확하면 최종 노출하지 않는다.

## 중복 병합

이벤트 ID 생성:

```text
normalize(title) + startDate + region
```

정규화:

- HTML 제거
- 괄호 안 부가 설명 제거
- 특수문자 제거
- 연속 공백 제거
- `축제`, `페스티벌`, `공연`, `콘서트` 같은 일반 단어만 남은 제목은 병합 기준으로 쓰지 않음

병합 규칙:

- TourAPI와 뉴스가 같은 제목/날짜면 하나로 합친다.
- TourAPI의 날짜/주소/이미지를 우선 사용한다.
- 뉴스 출처는 `sources`에 추가한다.
- 뉴스 언급 수는 점수에 반영한다.

## UI 구현

### `/events`

`app/events/page.tsx`를 추가한다.

정적 빌드 페이지로 둔다.

```ts
export const dynamic = "force-static";
```

구성:

- `SiteHeader`
- 상단 요약 영역
- 기간 표시: `YYYY-MM-DD ~ YYYY-MM-DD`
- 이벤트 카드 목록
- `SiteFooter`

문구 예:

```text
이번 주 문화 이벤트
뉴스 관심도와 공식 행사 정보를 함께 본 향후 7일 큐레이션
```

카드 정보:

- 순위
- 제목
- 카테고리
- 지역/장소
- 날짜
- 점수
- 선정 이유
- 출처 배지: `공식 행사`, `뉴스`
- 이미지가 있으면 표시

프론트엔드 주의:

- 기존 `kct-card`, `on-navy`, `text-accent` 스타일을 재사용한다.
- 카드 안에 또 카드를 넣지 않는다.
- 모바일에서 제목과 점수, 날짜가 겹치지 않게 한다.
- 너무 큰 히어로를 만들지 말고, 실제 이벤트 리스트가 첫 화면에 일부 보이게 한다.
- 이미지가 없을 때 깨진 `<img>`가 나오면 안 된다.

### 홈페이지

`app/page.tsx`에 최신 이벤트 섹션을 추가한다.

위치는 최신호 카드 아래, 구독 섹션 위가 적절하다.

표시:

- 최신 `events.json`의 상위 3개
- `/events`로 가는 링크
- 데이터가 없으면 섹션 자체를 숨긴다.

### 헤더

`components/SiteHeader.tsx`에 `/events` 링크를 추가한다.

예:

```text
이번 주 이벤트
```

## package.json 스크립트

추가:

```json
"events": "tsx scripts/pipeline/events/run-events.ts",
"events:collect": "tsx scripts/pipeline/events/01-collect-events.ts",
"events:analyze": "tsx scripts/pipeline/events/02-analyze-events.ts"
```

## make/publish 반영

`scripts/make.sh`:

- 이벤트 JSON을 먼저 생성한 뒤 기존 뉴스 issue 파이프라인을 실행한다.
- 실패 시 전체 제작 실패로 처리한다.
- 이유: 기존 `scripts/pipeline/run.ts`가 내부에서 `next build`를 실행하므로, 이벤트 파일은 빌드 전에 존재해야 한다.

권장 흐름:

```text
npm run events -- "$SLUG"
npm run pipeline -- "$SLUG"
```

`scripts/publish.sh`:

- 발행 전 `content/events/{slug}/events.json` 존재 여부를 확인한다.
- 없으면 경고 또는 실패 처리한다.
- `publishToGit(slug)`가 `content/issues/{slug}`와 `content/events/{slug}`를 함께 `git add`하도록 `scripts/pipeline/08-publish-send.ts`를 수정한다.
- 배포 폴링은 기존 `/issues/{slug}` 확인에 더해 `/events`도 확인한다.

권장:

- 운영 완성도를 위해 실패 처리한다.
- 단, 초기 도입 중에는 `ALLOW_MISSING_EVENTS=1`일 때만 경고 후 통과하도록 할 수 있다.

## 실행 명령

개발 검증:

```bash
npm run events -- 2026-06-03
npm run build
```

전체 제작:

```bash
./scripts/make.sh 2026-06-03
```

발행:

```bash
./scripts/publish.sh 2026-06-03
```

## 테스트 및 검수 기준

필수 검수:

```bash
npx tsc --noEmit
npm run build
git diff --check
```

이벤트 수집 검수:

```bash
npm run events -- 2026-06-03
```

확인할 파일:

```text
content/events/2026-06-03/events.json
```

검수 항목:

- `events.json`이 생성되는가
- `range.from`과 `range.to`가 정확한가
- 종료된 이벤트가 들어가지 않는가
- 7일 밖 이벤트가 들어가지 않는가
- 날짜 없는 뉴스 후보가 노출되지 않는가
- TourAPI 공식 행사가 들어오는가
- 뉴스 기반 대형 이벤트가 날짜 확인 시 들어오는가
- 점수순 정렬이 되는가
- `/events`가 빌드되는가
- 홈페이지에 최신 이벤트 3개가 표시되는가
- 이벤트 데이터가 없어도 빌드가 실패하지 않는가

## 오류 가능성이 높은 지점

### 1. TourAPI 응답 구조

공공데이터 응답은 `items.item`이 배열일 수도 있고 단일 객체일 수도 있다. 항상 배열로 정규화해야 한다.

### 2. 인증키

키는 환경변수에서 읽는다. 코드에 직접 쓰지 않는다.

401이 다시 나오면:

- `TOUR_API_SERVICE_KEY`가 설정되어 있는지 확인
- 일반 인증키가 들어갔는지 확인
- 키 앞뒤 공백 제거
- Swagger 미리보기에서 같은 키로 호출되는지 확인

### 3. 뉴스 날짜 추출

뉴스 기사 제목에 `다음 주`, `이번 주`만 있으면 실제 날짜가 아니다. 잘못된 이벤트 안내가 더 위험하므로 표시하지 않는다.

### 4. BTS 같은 대형 이벤트

BTS 공연은 지역축제가 아니지만 최상위가 될 수 있어야 한다. 따라서 `sourceType === "tour-api"`라는 이유만으로 우선순위를 높이면 안 된다. 최종 정렬은 점수로 한다.

### 5. 이미지

TourAPI `firstimage`가 없을 수 있다. 이미지가 없으면 카드 레이아웃이 깨지지 않게 텍스트형 카드로 표시한다.

## 구현 순서

1. `types/event.ts` 추가
2. `lib/tour-api.ts` 추가
3. `lib/events.ts` 추가
4. `scripts/pipeline/events/01-collect-events.ts` 추가
5. `scripts/pipeline/events/02-analyze-events.ts` 추가
6. `scripts/pipeline/events/run-events.ts` 추가
7. `package.json` scripts 추가
8. `components/EventView.tsx` 추가
9. `app/events/page.tsx` 추가
10. `components/SiteHeader.tsx` 링크 추가
11. `app/page.tsx` 최신 이벤트 섹션 추가
12. `lib/paths.ts`에 이벤트 경로 헬퍼 추가
13. `scripts/make.sh`에 빌드 전 이벤트 생성 추가
14. `scripts/publish.sh`에 이벤트 파일 확인 추가
15. `scripts/pipeline/08-publish-send.ts`가 `content/events/{slug}`도 커밋하도록 수정
16. `npm run events -- {slug}` 실행
17. `npx tsc --noEmit`
18. `npm run build`
19. `git diff --check`

## 코드리뷰 1차 결과

검토 기준: 기존 프로젝트 구조와 충돌 여부.

수정/보완 사항:

- 이벤트 로더는 `lib/content.ts`에 섞지 않고 `lib/events.ts`로 분리한다. 뉴스 issue와 이벤트 문서는 데이터 성격이 다르기 때문이다.
- `scripts/make.sh`는 이벤트 JSON 생성을 `scripts/pipeline/run.ts`보다 먼저 실행해야 한다. 기존 파이프라인이 내부에서 `next build`를 수행하기 때문이다.
- `scripts/publish.sh`는 재생성하지 않는 발행 스크립트이므로 이벤트 파일 존재 확인만 한다.
- `scripts/pipeline/08-publish-send.ts`의 `publishToGit()`는 `content/events/{slug}`도 `git add`해야 한다. 현재 구조처럼 `content/issues/{slug}`만 add하면 이벤트 페이지 데이터가 배포되지 않는다.
- API 키 원문은 문서와 코드에 넣지 않고 `TOUR_API_SERVICE_KEY`만 사용한다.

판정: 구조상 합의 가능.

## 코드리뷰 2차 결과

검토 기준: 데이터 정확성.

수정/보완 사항:

- 이미 시작했지만 이번 주에도 진행 중인 이벤트를 놓치지 않도록 TourAPI 수집 범위를 `slug - 90일`부터 `slug + 7일`까지 넓힌다.
- 최종 노출은 `startDate <= displayTo && endDate >= displayFrom` 조건으로 직접 필터링한다.
- `endDate`가 없으면 `startDate`와 같은 날 종료되는 이벤트로 보정한다.
- TourAPI 페이지네이션은 `totalCount` 기준으로 끝까지 수집하되 최대 20페이지 제한을 둔다.
- 뉴스 기반 후보는 날짜가 명확하지 않으면 최종 노출하지 않는다. 이 조건이 없으면 존재하지 않거나 날짜가 틀린 이벤트가 `/events`에 표시될 수 있다.
- TourAPI 공식 데이터는 날짜/주소/이미지를 우선 사용한다.
- 중복 병합 시 뉴스 출처를 버리지 말고 `sources`에 보존한다.
- `items.item` 단일 객체 응답 가능성을 반드시 처리한다.

판정: 날짜 신뢰도 조건을 지키면 합의 가능.

## 코드리뷰 3차 결과

검토 기준: 사용자 경험과 운영 안정성.

수정/보완 사항:

- `/events`는 데이터가 없을 때도 빈 상태 화면으로 빌드되어야 한다.
- `/events`에는 `export const dynamic = "force-static";`를 둔다.
- 홈페이지 최신 이벤트 섹션은 데이터가 없으면 숨긴다.
- 이미지 없는 이벤트 카드 레이아웃을 별도로 처리한다.
- `make.sh`의 실행 순서는 `events -> pipeline`이어야 한다.
- `publishToGit()`의 커밋 대상은 `content/issues/{slug}`와 `content/events/{slug}` 둘 다여야 한다.
- 날짜 계산은 한국 기준 문자열로 처리하고 `new Date(slug).toISOString()` 방식은 금지한다.
- `ALLOW_MISSING_EVENTS=1` 예외는 초기 도입 때만 사용하고, 운영에서는 이벤트 파일 누락을 실패로 보는 것이 맞다.
- `npx tsc --noEmit`, `npm run build`, `git diff --check`를 최종 검수에 포함한다.

판정: 위 조건을 반영한 구현이면 합의 가능.

## 최종 합의안

Claude는 이 문서를 기준으로 구현하면 된다.

최종 구현의 핵심 원칙:

- TourAPI는 공식 행사 기반 데이터로 사용한다.
- 네이버 뉴스는 대형 이벤트와 관심도 보강용으로 사용한다.
- BTS 공연 같은 대형 이벤트는 지역축제가 아니어도 점수에 따라 1위가 될 수 있어야 한다.
- 날짜가 불명확한 뉴스 이벤트는 노출하지 않는다.
- 이미 시작했지만 이번 주에도 진행 중인 이벤트는 놓치지 않는다.
- API 키는 환경변수로만 관리한다.
- `/events`는 정적 빌드 가능한 페이지로 만든다.
- `make.sh`에는 빌드 전 이벤트 생성, `publish.sh`에는 이벤트 파일 존재 검증을 추가한다.
- 발행 커밋에는 `content/events/{slug}`가 반드시 포함되어야 한다.

최종 검수 명령:

```bash
npm run events -- 2026-06-03
npx tsc --noEmit
npm run build
git diff --check
```
