# 뉴스 우선순위 분석 최종 합의 구현안

## 결론

`지피1.md`의 큰 방향은 채택한다. 다만 `클로1.md`의 검토 의견을 반영해 1차 구현은 더 가볍고 안정적인 방식으로 간다.

최종 합의안은 다음이다.

```text
collect -> analyze(코드 휴리스틱) -> curate(LLM 1회) -> images -> issue.json
```

핵심 원칙:

- 외부 API나 별도 크롤링을 추가하지 않는다.
- 현재 수집된 `raw.items` 전체를 분석한다.
- 분석 단계에서는 LLM을 호출하지 않는다.
- LLM 호출은 기존 `02-curate.ts`의 큐레이션 1회만 유지한다.
- 보도량은 원시 기사 수보다 서로 다른 언론사 수를 더 중요하게 본다.
- 점수는 절대값이 아니라 "수집 뉴스 기반 우선순위 추정치"로 취급한다.
- 기존 `issue.json` 렌더링이 깨지지 않게 optional 필드로 확장한다.

## 현재 문제

현재 프로젝트는 `scripts/pipeline/02-curate.ts`에서 LLM에게 "중요한 뉴스를 골라라"라고 맡기는 구조다. 프롬프트에는 중요도 기준이 있지만, 각 뉴스가 왜 우선순위가 높은지 점수화하거나 검증하는 별도 분석 단계는 없다.

또한 `types/pipeline.ts`에는 `AnalyzedIssue`와 `Analysis` 타입이 존재하지만 실제 파이프라인에서는 사용되지 않는다. 따라서 지금은 "정성 큐레이션"은 되지만 "우선순위 분석 엔진"이라고 보기는 어렵다.

가장 큰 개선 지점은 현재 수집된 뉴스 풀 안에 이미 존재하는 무료 신호를 살리는 것이다.

- 같은 이슈를 다룬 기사 수
- 서로 다른 언론사 수
- 최근 발행 집중도
- 제목/설명의 산업 파급 키워드
- 제목/설명의 심리 자극 키워드
- 대형 인물, 기업, 작품, 브랜드명

단, 이 신호들은 실제 검색량이나 SNS 반응이 아니다. 반드시 "수집 뉴스 기반 추정치"로 다뤄야 한다.

## 최종 아키텍처

### 1. 새 분석 단계 추가

새 파일:

```text
scripts/pipeline/02-analyze.ts
```

역할:

- `RawNews` 전체를 입력받는다.
- 기사들을 보수적으로 이슈 단위로 묶는다.
- 이슈별 기사 수, 매체 수, 최신성, 키워드 점수를 계산한다.
- 종합점수 기준으로 정렬한다.
- `Analysis`를 반환한다.
- CLI 실행 시 `.pipeline-tmp/{slug}/analysis.json`에 저장한다.

함수 시그니처:

```ts
export function analyze(raw: RawNews): Analysis
```

중요: `analyze`는 LLM을 호출하지 않는다. 순수 코드 함수로 만든다.

### 2. 파이프라인 순서 변경

`scripts/pipeline/run.ts`를 아래 흐름으로 바꾼다.

현재:

```text
collect -> curate -> images -> gate -> build/publish
```

변경:

```text
collect -> analyze -> curate -> images -> gate -> build/publish
```

예상 코드 흐름:

```ts
import { analyze } from "./02-analyze";

const analysis = analyze(raw);
writeJson(`${tmpDir(slug)}/analysis.json`, analysis);

const curated = await curate(raw, analysis);
```

### 3. 큐레이션 단계 변경

`scripts/pipeline/02-curate.ts`의 함수 시그니처를 확장한다.

```ts
export async function curate(
  raw: RawNews,
  analysis?: Analysis,
): Promise<CuratedDoc>
```

분석 결과가 있으면 기존의 단순 기사 목록 대신, 상위 분석 이슈를 LLM 프롬프트에 제공한다.

LLM에게 제공할 정보:

- 이슈 rank
- heading
- category
- score
- importance / impact / interest / psychological / realtime
- sourceCount
- outletCount
- 대표 기사 제목/설명/링크 인덱스
- rationale

큐레이션 LLM의 역할:

- 분석 점수와 근거를 참고해 카테고리별 대표 뉴스를 고른다.
- 보도량이 많아도 홍보성/선정성/저가치 이슈는 제외한다.
- 같은 이슈 중복은 대표 기사 1건만 고른다.
- 편집장 픽은 최상위권 이슈 중 문화적 의미가 가장 큰 것으로 고른다.
- 최종 blurb, editorial, selectionRationale을 작성한다.

중요: LLM이 순위를 완전히 새로 만들게 하지 않는다. 코드가 만든 점수와 근거를 강한 참고 신호로 주되, 문화적 의미와 저가치 기사 제외는 LLM이 보정하게 한다.

## 타입 변경안

### `types/pipeline.ts`

기존 `AnalyzedIssue`의 `intensities`와 새 점수 필드가 중복되지 않도록 정리한다. 아래 형태를 추천한다.

```ts
export interface IssueScores {
  /** 산업/문화적으로 중요한 정도 */
  importance: number;
  /** 국내외·산업·시장 파급 정도 */
  impact: number;
  /** 수집 뉴스 풀 안에서 관측된 관심도 추정 */
  interest: number;
  /** 감정 반응을 유발할 가능성. 클릭베이트 방지를 위해 낮은 가중치로 사용 */
  psychological: number;
  /** 최근성·보도 집중도·주간 지속성 */
  realtime: number;
}

export interface AnalyzedIssue {
  rank: number;
  heading: string;
  category: string;
  summary: string;
  scores: IssueScores;
  /** 종합 점수 0-100. 수집 뉴스 기반 추정치 */
  score: number;
  rationale: string;
  sourceIndexes: number[];
  sourceCount: number;
  outletCount: number;
  outlets: string[];
  primaryIndex: number;
}

export interface Analysis {
  analyzedAt: string;
  method: "local-heuristic-v1";
  note: string;
  issues: AnalyzedIssue[];
}
```

`intensities`는 제거하거나 더 이상 사용하지 않는다. 하위 호환이 걱정되면 optional로 둘 수 있지만, 새 코드에서는 `scores`만 사용한다.

### `types/issue.ts`

렌더링이 깨지지 않도록 optional 필드로 확장한다.

```ts
export interface NewsPriorityAnalysis {
  rank: number;
  score: number;
  scores: IssueScores;
  rationale: string;
  sourceCount: number;
  outletCount: number;
}

export interface NewsEntry {
  // 기존 필드 유지
  analysis?: NewsPriorityAnalysis;
}

export interface EditorPick {
  // 기존 필드 유지
  analysis?: NewsPriorityAnalysis;
}

export interface IssueMeta {
  // 기존 필드 유지
  analysis?: {
    method: "local-heuristic-v1";
    note: string;
    issueCount: number;
  };
}
```

주의: `NewsPriorityAnalysis.scores`는 `types/pipeline.ts`의 `IssueScores`를 import해서 사용한다. 중복 타입을 만들지 않는다.

## 분석 로직 상세

### 1. 전체 raw.items를 먼저 분석한다

현재 `02-curate.ts`는 `PER_CAT`, `MAX_INPUT`으로 후보를 자른 뒤 LLM에 전달한다. 하지만 우선순위 분석은 자르기 전 전체 `raw.items`를 대상으로 해야 한다.

분석 순서:

```text
raw.items 전체
-> 링크 중복 제거
-> 보수적 이슈 클러스터링
-> 이슈별 점수 계산
-> score 정렬
-> 상위 이슈를 curate에 전달
```

`CURATE_PER_CAT`, `CURATE_MAX_INPUT`은 분석 전에 적용하지 않는다. 큐레이션 LLM 입력 길이를 줄일 때는 분석 결과의 상위 이슈를 대상으로 적용한다.

### 2. 보수적 클러스터링

외부 임베딩이나 형태소 분석기를 추가하지 않는다. 로컬 문자열 휴리스틱만 사용한다.

권장 방식:

- 제목 + 설명을 합쳐 정규화한다.
- HTML, 특수문자, 따옴표, 괄호, 과도한 공백을 제거한다.
- 2글자 이하 토큰은 버린다.
- 너무 흔한 단어는 stopword로 버린다.
- 같은 카테고리 안에서만 묶는 것을 기본값으로 한다.
- 핵심 토큰 겹침이 높은 경우에만 같은 이슈로 묶는다.
- `BTS`, `블랙핑크`, `넷플릭스`, `올리브영` 같은 대형 키워드 하나만 같다고 묶지 않는다.

권장 임계값:

```text
같은 카테고리 AND
공통 핵심 토큰 2개 이상 AND
Jaccard 유사도 0.35 이상
```

임계값은 첫 실주행 후 조정한다. 클러스터링은 완벽하지 않으므로, 보도량 점수는 하드 정렬 기준이 아니라 소프트 신호로만 쓴다.

### 3. outlet diversity 중심

원시 기사 수는 홍보성 보도자료 재탕에 취약하다. 따라서 `interest`는 `sourceCount`보다 `outletCount`를 더 크게 반영한다.

예시:

```ts
interest =
  outletDiversityScore * 0.65 +
  sourceVolumeScore * 0.25 +
  topicConcentrationScore * 0.10
```

동일 매체가 같은 이슈를 여러 번 낸 경우 가중치를 낮춘다.

### 4. 점수 모델

점수는 0~100 범위로 clamp한다.

추천 가중치:

```ts
score =
  scores.importance * 0.32 +
  scores.impact * 0.27 +
  scores.interest * 0.23 +
  scores.psychological * 0.08 +
  scores.realtime * 0.10
```

이 가중치는 임시값이다. 거짓 정밀도를 피하기 위해 코드 주석에 "v1 heuristic weights"라고 명시한다.

심리강도는 클릭베이트를 상위로 올릴 위험이 있으므로 0.08로 낮게 둔다. 심리 키워드가 있어도 중요도와 파급력이 낮으면 종합점수가 크게 오르지 않게 한다.

### 5. 세부 점수 기준

`importance`

- 기록, 수상, 차트, 흥행, 출시, 투자, 인수, 계약, 선판매, 글로벌 계약, 산업 변화 키워드가 있으면 상승
- 주요 아티스트, 작품, 기업, 플랫폼, 브랜드가 있으면 상승
- 단순 화보, 근황, 출연, 신제품 나열, 보도자료 냄새가 강하면 감점

`impact`

- 글로벌, 해외, 북미, 일본, 중국, 수출, 시장, 플랫폼, 팬덤, 대규모 관객, 매출, 1위, 100만, 500개, 국가/권역 등 파급 키워드가 있으면 상승
- 산업 구조나 시장 확장과 연결되면 상승

`interest`

- 서로 다른 언론사 수가 많으면 상승
- 같은 이슈 기사 수가 많으면 상승
- 동일 제목/보도자료 반복이 강하면 상승폭을 제한

`psychological`

- 논란, 반발, 열풍, 품절, 기대, 깜짝, 최초, 압도, 돌풍, 위기 등 감정 자극 키워드가 있으면 소폭 상승
- 외모/자태/직캠/포착류 클릭베이트는 기존 `news-filter.ts`와 분석 감점 로직으로 낮춘다

`realtime`

- 최근 24~48시간 기사 비율이 높으면 상승
- 주간 매거진 특성을 고려해 한 주 내 꾸준히 보도된 이슈도 일부 가산
- 가중치는 낮게 유지한다

## `02-curate.ts` 입력 구성

분석 결과가 있을 때 LLM 입력은 기사 전체가 아니라 분석 상위 이슈 중심으로 구성한다.

추천 입력 상한:

```text
상위 이슈 전체 80개 이내
카테고리별 최소 8개 이슈 보장
각 이슈당 대표 기사 최대 3개
```

기존 환경변수 호환:

- `CURATE_MAX_INPUT`은 LLM에 넣을 대표 기사 총량 상한으로 유지한다.
- `CURATE_PER_CAT`은 카테고리별 분석 이슈 상한 또는 대표 기사 상한으로 재해석한다.
- 환경변수 이름은 당장 바꾸지 않는다.

LLM 프롬프트에 반드시 포함할 문장:

```text
아래 점수는 외부 검색량/SNS 반응이 아니라 수집 뉴스 풀 기반 추정치다.
원시 기사 수보다 서로 다른 언론사 수와 문화적 의미를 더 신뢰하라.
홍보성 보도자료, 선정적 클릭베이트, 단순 근황은 점수가 높아도 제외하라.
```

LLM 출력은 기존 `CurateSchema`를 유지하되, `issueRank`를 optional로 추가한다.

인덱스 기준은 반드시 명확히 한다.

- LLM 프롬프트에 보여주는 후보 목록은 `candidateItems` 같은 별도 배열로 만든다.
- `CurateEntry.index`는 기존처럼 "LLM에게 보여준 후보 목록의 0부터 시작하는 인덱스"로 유지한다.
- 후보 목록을 만들 때 `candidateIndex -> rawIndex -> issueRank` 매핑을 별도로 보관한다.
- `sourceIndexes`와 `primaryIndex`는 항상 `raw.items` 원본 인덱스다.
- 조립 단계에서는 `CurateEntry.index`를 먼저 `rawIndex`로 변환한 뒤 `NewsEntry`와 분석 점수를 붙인다.

```ts
export const CurateEntrySchema = z.object({
  index: z.number().int(),
  issueRank: z.number().int().optional(),
  blurb: z.string(),
  imagePrompt: z.string(),
  imageQuery: z.string().optional(),
});
```

`issueRank`가 있으면 조립 단계에서 해당 분석 점수를 `NewsEntry.analysis`에 붙인다. 없으면 `candidateIndex -> rawIndex` 매핑 후 `sourceIndexes`를 역조회해 붙인다.

## `issue.json` 저장 방식

발행 JSON에는 전체 `analysis.issues`를 모두 넣지 않는다. 파일이 불필요하게 커지고 렌더링 관심사와 섞인다.

저장 권장:

- `meta.analysis`: 분석 방식과 총 이슈 수
- 각 `NewsEntry.analysis`: 선택된 뉴스의 점수와 근거
- `editorPick.analysis`: 편집장 픽 점수와 근거

예시:

```json
{
  "analysis": {
    "rank": 1,
    "score": 91,
    "scores": {
      "importance": 93,
      "impact": 94,
      "interest": 86,
      "psychological": 50,
      "realtime": 78
    },
    "rationale": "8개 매체가 보도했고 글로벌 투어 성과와 시장 파급 키워드가 강함.",
    "sourceCount": 12,
    "outletCount": 8
  }
}
```

## UI 반영안

1차 구현에서는 UI를 과하게 바꾸지 않는다.

추천 노출:

- 홈 상단: `수집 뉴스 기반 우선순위 분석`
- 카드 또는 상세: `선정 근거: 12개 기사 · 8개 매체`
- 선택 노출: `중요도 91 · 파급력 88 · 관심도 84`

반드시 포함할 표현:

```text
수집 뉴스 기반 추정치
```

이 문구 없이 점수를 보여주면 실제 검색량이나 SNS 반응처럼 오해될 수 있다.

## 구현 순서

1. `types/pipeline.ts`에서 `IssueScores`, 확장된 `AnalyzedIssue`, `Analysis` 정리
2. `scripts/pipeline/02-analyze.ts` 추가
3. `package.json`에 `pipeline:analyze` 스크립트 추가
4. `scripts/pipeline/run.ts`에 analyze 단계 연결
5. `scripts/pipeline/02-curate.ts` 시그니처와 프롬프트 변경
6. `lib/schemas.ts`에 `issueRank` optional 추가
7. `types/issue.ts`에 optional 분석 메타 추가
8. 선택된 entry/editorPick에 분석 점수 attach
9. 필요한 경우 `components/IssueView.tsx`, `app/page.tsx`에 근거 문구 표시
10. `npx next build`로 검증

## 테스트와 검증

필수 검증:

```bash
npx tsc --noEmit
npx next build
```

가능하면 아래도 확인한다.

```bash
LLM_MODE=mock npm run pipeline
```

수동 검수 체크리스트:

- `.pipeline-tmp/{slug}/analysis.json`이 생성되는가
- 분석 이슈가 `score` 내림차순인가
- 같은 보도자료 반복이 과도하게 상위로 올라오지 않는가
- 대형 글로벌/산업 이슈가 상위권에 오는가
- 단순 화보/근황/출연 기사가 다시 살아나지 않는가
- `content/issues/{slug}/issue.json`이 기존 화면 렌더링을 깨지 않는가
- selected/news count가 기존과 비슷한 범위로 유지되는가

## 비범위

1차 구현에서 하지 않는다.

- Google Trends, Naver DataLab, SNS API 연동
- 댓글 수, 조회 수 크롤링
- 외부 임베딩 API
- 형태소 분석기 패키지 추가
- LLM을 두 번 호출하는 분석 보정 단계
- 과도한 점수 UI 대시보드

## 구현 시 주의사항

- `analyze`는 deterministic하게 만든다. 같은 `raw.items` 입력이면 같은 결과가 나와야 한다.
- 클러스터링 실패 가능성을 전제로 한다. 보도량 점수만으로 최종 선정하지 않는다.
- `sourceIndexes`는 `raw.items` 원본 인덱스를 유지해야 한다.
- `primaryIndex`는 `raw.items` 원본 기준 대표 기사 인덱스다.
- `CurateEntry.index`는 기존 호환을 위해 LLM 후보 배열 기준 인덱스로 유지하고, 별도 매핑으로 원본 인덱스에 연결한다.
- `outletFromUrl`은 이미 `lib/og-image.ts`에 있으므로 재사용한다.
- `shouldExclude`는 이미 `lib/news-filter.ts`에 있으므로 분석 단계에서도 저가치 감점 또는 제외에 재사용한다.
- 기존 mock fallback을 깨지 않는다.
- 기존 `content/issues/*/issue.json`은 마이그레이션하지 않는다. 새 필드는 optional로만 추가한다.

## 최종 합의 요약

이 구현의 목표는 "AI가 그냥 골랐다"에서 "수집된 뉴스 풀을 근거로 우선순위를 계산하고, LLM이 그 근거를 바탕으로 최종 큐레이션했다"로 프로젝트의 설명력을 높이는 것이다.

최종 방식은 `지피1.md`의 분석 엔진 방향을 유지하되, `클로1.md`의 지적대로 LLM 2회 호출은 피한다. 즉, 1차 분석은 코드가 맡고, 기존 큐레이션 LLM 1회가 문화적 의미와 저가치 기사 제외를 최종 보정한다.

이 방식이 현재 코드베이스에 가장 잘 맞고, 구현 비용 대비 품질 개선 효과가 크다.

## 문서 코드리뷰 기록

### 1차 리뷰

점검 항목:

- `클로1.md`의 핵심 이견인 LLM 1회 유지가 반영됐는가
- 분석이 `raw.items` 전체에서 먼저 수행되도록 명시됐는가
- 보도량보다 매체 다양성을 우선하도록 명시됐는가
- 심리강도 가중치가 낮아졌는가
- 기존 렌더링을 깨지 않도록 optional 필드로 설계됐는가

결과:

- 모두 반영됨.

### 2차 리뷰

점검 항목:

- `sourceIndexes`와 `primaryIndex`가 `raw.items` 원본 인덱스 기준임을 명시했는가
- `CURATE_MAX_INPUT`, `CURATE_PER_CAT` 기존 환경변수 호환이 깨지지 않도록 설명했는가
- 전체 `analysis.issues`를 `issue.json`에 저장하지 않도록 제한했는가
- 외부 API, 크롤링, 형태소 분석기 추가가 비범위로 명시됐는가

결과:

- 모두 반영됨.

### 3차 리뷰

점검 항목:

- 구현자가 바로 수정할 파일 목록과 순서를 알 수 있는가
- 필수 검증 명령이 포함됐는가
- 점수가 실제 대중 반응이 아니라 추정치라는 경고가 UI/문서 양쪽에 들어갔는가
- `AnalyzedIssue.intensities`와 새 `scores`의 중복 정리 방침이 명확한가

결과:

- 모두 반영됨. 현재 문서는 구현 의뢰용 최종안으로 사용 가능함.

### 4차 리뷰

점검 항목:

- `CurateEntry.index`, `raw.items` 원본 인덱스, `issueRank` 사이의 매핑이 혼동되지 않게 설명됐는가
- 느슨한 구현 지시 표현이 남아 있지 않은가
- `IssueScores` 타입 중복 생성 대신 import 사용 방침이 명확한가
- 문서 패치에 trailing whitespace 같은 형식 오류가 없는가

결과:

- 모두 반영됨.
- `git diff --check -- 지피2.md` 통과.
- 최종 구현 의뢰 문서로 사용 가능함.
