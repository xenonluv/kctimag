# 수동 제작·발행 매뉴얼 — 주간 한국문화 AI 큐레이션 뉴스모음 (KCTI)

> 매주 새 호를 **① 제작·검수 → ② 발행 → ③ 메일** 순서로 직접 발행하는 방법.

## ⚡ 한 줄 요약
```
./scripts/make.sh 2026-06-08      # ① 제작 후 localhost에서 검수
./scripts/publish.sh 2026-06-08   # ② 마음에 들면 push → Vercel 발행
#  ③ /admin 에서 구독자 메일 수동 발송
```
> 날짜(`2026-06-08`)가 곧 그 호의 URL(`/issues/2026-06-08`)이 됩니다. 생략하면 "오늘 날짜".
> npm 별칭도 가능: `npm run make 2026-06-08`, `npm run publish 2026-06-08`.

---

## ① 제작·검수 — `make.sh`
```
cd ~/kctimag            # (이 PC는 WSL 안에서) / Mac은 프로젝트 폴더로
./scripts/make.sh 2026-06-08
```
- **하는 일**: 네이버 수집 → AI(codex) 큐레이션 → Pexels 실사진 → 조립 → CEO 검증 → 빌드 → **검수용 로컬 서버 실행**.
- **검수**: 브라우저에서 **http://localhost:3000** (홈)과 **/issues/2026-06-08** (호 상세)를 열어 확인.
  - 홈: 상단 **"AI 엄선 카드뉴스" 자랑 카드** — 분석 N건 → 선별 N건 + **AI 선정 이유 3줄**(큐레이터가 매번 새로 작성).
  - 호 상세: 편집장 픽 · 총평 · 카테고리별 카드뉴스 + **카테고리마다 "이번 주 선정 이유" 노트**.
- **종료**: 터미널에서 **Ctrl+C**.
- **마음에 안 들면**: Ctrl+C 후 `make.sh` 다시 실행 → 큐레이션을 새로 함(선별·문구가 매번 조금 달라짐). 만족할 때까지 반복.
- ⚠️ 이 단계는 **GitHub push도, 메일도 하지 않음.** 로컬에만 만들어 검수만 합니다.

## ② 발행 — `publish.sh`
```
./scripts/publish.sh 2026-06-08
```
- **하는 일**: 검수한 그 호를 **재생성 없이** GitHub push → Vercel 자동 배포 → PDF 생성·업로드 → 최근 8호만 보관(자동 정리).
- 안전장치: push 시 **그 호 폴더(`content/issues/2026-06-08`)만** 커밋 → 작업 폴더에 남은 다른 검수/테스트 호가 같이 발행되는 사고 방지.
- **구독자 메일은 보내지 않음** (다음 단계에서 수동).
- 완료 후 라이브: **https://kctimag.vercel.app/issues/2026-06-08** (홈 최신호 히어로도 이 호로 자동 교체).

## ③ 구독자 메일 — 관리자 페이지에서 수동
1. **https://kctimag.vercel.app/admin/login** → 관리자 비밀번호 입력.
2. "호 발송 / 테스트" 섹션에서 해당 호 폼(위→아래 순서):
   - **메일 제목**: 기본값 `KCTI - AI 큐레이터 발행 주간 문화 뉴스 모음 (2026-06-08)`가 미리 채워짐 → 그 자리에서 수정 가능. 비우면 기본값 사용.
   - **내용**: 메일 본문 직접 작성(비우면 부제가 본문).
   - **색**: 비우면 발행 주차별 7색 자동 순환(라벤더→로즈→민트→스카이블루→코랄→그린→앰버), 직접 고르면 그 색.
   - **보낸사람 이름**: 원하는 발신자명(비우면 `MAIL_FROM` 기본값).
   - **테스트 이메일**: 내 주소 입력 → [발송] = **나에게만**(테스트). 비우면 → **전체 확인 구독자**.
   - PDF는 자동 첨부됨.
3. 먼저 **내 주소로 테스트** → 괜찮으면 테스트 칸 비우고 **전체 발송**.

### 메일에 항상 들어가는 것
- **7색 파스텔 카드** 디자인(주차별 자동 순환) · 첨부 PDF
- **"웹에서 전체 보기"** 버튼 → **홈(`/`)** 으로(AI 엄선 자랑 카드를 먼저 보여줌)
- **구독 취소 버튼** + 안내문 — 누르면 즉시 수신거부 처리(이후 발송 대상에서 자동 제외)
  - 단, **테스트 발송**의 취소 버튼은 그 주소가 실제 구독자일 때만 동작(아니면 홈으로 이동).
  - 수신거부했거나 취소 테스트한 사람은 **홈 구독 폼에서 다시 구독하면 복구**됨.

---

## 사전 준비 (최초 1회)
- 터미널: 이 PC = **WSL(Ubuntu)** 안에서 / Mac = zsh.
- `npm install` (의존성), `.env.local` 키 일체(Naver·Supabase·Brevo·Pexels 등) + `MAIL_FROM`(보낸사람 표시 이름·주소).
- 큐레이션 AI 로그인: 이 PC = **codex** 로그인 상태 / Mac = codex 또는 `LLM_PROVIDER=claude ./scripts/make.sh ...`.
- PDF용 Chrome: 이 PC 설치 완료 / Mac 기본.
- GitHub push 인증: 이 PC는 보통 Windows git으로 push.

## 문제 해결 / 메모
- **이 PC(WSL)에서 `publish.sh`의 git push가 안 되면**: `make.sh`로 검수까지만 하고, push는 **Windows 터미널**에서 — ⚠️ 그 호 폴더만:
  ```
  git add content/issues/2026-06-08 && git commit -m "발행: 2026-06-08호" && git push
  ```
  (`git add content/` 전체로 하지 말 것 — 다른 테스트 호가 섞일 수 있음. 그 뒤 PDF는 `PDF_BASE_URL=https://kctimag.vercel.app npx tsx scripts/pipeline/06-pdf.ts 2026-06-08` 로 생성·업로드.)
- **푸시했는데 라이브에 반영이 안 되면**(Vercel 자동배포 웹훅 누락): 빈 커밋으로 재배포 트리거 —
  ```
  git commit --allow-empty -m "chore: redeploy" && git push
  ```
  또는 Vercel 대시보드 → **Deployments** 에서 최신 커밋 상태(Ready/Building/Error) 확인.
- **메일·이미지**: 이미지는 핫링크(저장 안 함), PDF는 최근 8호만 Supabase 보관 → 무료 한도 안전. 보낸사람 표시 이름을 영구 변경하려면 Vercel 환경변수 `MAIL_FROM` 수정.
- **자동(한 방) 발행**(메일까지 자동): `PUBLISH=1 LLM_PROVIDER=codex npx tsx scripts/pipeline/run.ts 2026-06-08` — ⚠️ 검토 없이 구독자에게 즉시 메일이 나가므로 **평소엔 절대 쓰지 말고** 위 ①~③ 권장.
- **Mac 완전 자동화**: launchd로 주 1회 자동 발행 가능(`맥투두.md` 참고).

---
**흐름:  make.sh(검수) → [반복] → publish.sh(웹 발행) → /admin(메일)**
