# 향후 7일 문화 이벤트 큐레이션 페이지 구현 방안

## 목표

현재 프로젝트는 지난 1주일간의 한국 문화 뉴스를 수집하고, 중요도/파급력/관심도 기준으로 큐레이션한다. 여기에 별도 페이지를 추가해 **앞으로 7일 안에 열릴 문화 이벤트**를 미리 안내한다.

핵심은 단순 지역축제 목록이 아니다. BTS 공연, 블랙핑크 콘서트, 대형 전시, 영화제, e스포츠 결승, 국립기관 특별전, 지역 대표 축제처럼 **향후 7일 안에 실제로 열리고 관심도가 높은 문화 이벤트**를 중요도순으로 보여주는 페이지를 만든다.

최종 사용자 경험:

```text
/events
이번 주 놓치면 아쉬운 문화 이벤트
뉴스성과 관심도, 임박성, 공식 출처를 기준으로 정렬
```

## 핵심 방향

이 기능은 "지역축제 페이지"가 아니라 "다가올 문화 이벤트 큐레이션"이어야 한다.

따라서 데이터 소스는 두 갈래로 둔다.

1. **네이버 뉴스 기반 대형 이벤트 후보**
   - BTS 공연, 대형 콘서트, 영화제, 전시, 팝업, e스포츠, 팬미팅, 콘텐츠 행사 등
   - 뉴스에 언급될 정도로 관심도와 파급력이 큰 이벤트를 잡는다.
   - 이 소스가 메인이다.

2. **문체부 지역축제 공식 페이지**
   - `https://www.mcst.go.kr/site/s_culture/festival/festivalList.jsp`
   - 지역 축제 커버리지 보완용이다.
   - 문체부 페이지 안내에 따르면 하단 축제정보는 한국관광공사 대한민국 구석구석 정보가 연동된 것이다.

이 두 소스를 합쳐서 점수화한다.

```text
뉴스 기반 대형 이벤트 후보
+ 문체부 지역축제 후보
-> 중복 병합
-> 향후 7일 필터
-> 중요도/관심도/임박성 점수화
-> /events 페이지 렌더링
```

## 중요한 판단

문체부 지역축제만 사용하면 부족하다. BTS 공연이나 대형 K-pop 콘서트는 지역축제가 아니지만 실제 관심도와 파급력 기준으로는 최상위가 될 수 있다.

따라서 우선순위는 다음과 같이 잡는다.

- 대형 아티스트/브랜드/기관 이벤트는 최상위 후보가 될 수 있어야 한다.
- 문체부 지역축제는 공식성과 지역 관광성을 보강하는 소스다.
- 최종 정렬은 출처 종류가 아니라 점수로 한다.

예:

```text
BTS 서울 공연
-> 대형 아티스트 + 글로벌 관심 + 뉴스 언급량 + 임박성
-> 지역축제가 아니어도 1위 가능

전주국가유산야행
-> 공식 축제 출처 + 지역 관광성 + 임박성 + 뉴스 언급량
-> 상위권 가능
```

## 1차 구현 범위

1차는 아래 범위로 제한한다.

- `/events` 페이지 추가
- 향후 7일 이벤트만 표시
- 기존 네이버 뉴스 API를 활용한 이벤트 후보 추출
- 문체부 지역축제 페이지 크롤링
- 이벤트별 중요도/관심도/임박성 점수 계산
- `content/events/{slug}/events.json` 저장
- 정적 렌더링 가능하게 구현

1차에서 하지 않는다.

- 인터파크/멜론티켓/예스24 전체 크롤링
- 전국 모든 공연장/미술관 사이트 통합
- 사용자 지역 개인화
- 티켓 잔여석/가격 실시간 추적
- SNS 반응 수집
- Google Trends/Naver DataLab 연동

## 추천 URL 구조

```text
/events
```

데이터 파일:

```text
content/events/{slug}/events.json
```

예:

```text
content/events/2026-06-08/events.json
```

`slug`는 뉴스 issue와 같은 발행 기준 날짜를 사용한다.

## 타입 설계

새 파일을 추가한다.

```text
types/event.ts
```

추천 타입:

```ts
export type EventSourceType = "news" | "mcst-festival";

export interface EventSourceRef {
  type: EventSourceType;
  title: string;
  url: string;
  outlet?: string;
  pubDate?: string;
}

export interface EventScores {
  importance: number;
  interest: number;
  urgency: number;
  scale: number;
  reliability: number;
}

export interface CultureEvent {
  id: string;
  title: string;
  category: "concert" | "festival" | "exhibition" | "performance" | "film" | "esports" | "popup" | "heritage" | "other";
  region?: string;
  venue?: string;
  startDate: string;
  endDate?: string;
  description?: string;
  officialUrl?: string;
  sources: EventSourceRef[];
  scores: EventScores;
  score: number;
  rationale: string;
  imageQuery?: string;
  imagePrompt?: string;
}

export interface EventsDoc {
  slug: string;
  generatedAt: string;
  range: { from: string; to: string };
  meta: {
    title: string;
    dek: string;
    totalCandidates: number;
    selected: number;
    note: string;
  };
  events: CultureEvent[];
}
```

## 파이프라인 설계

새 스크립트:

```text
scripts/events/01-collect-events.ts
scripts/events/02-analyze-events.ts
scripts/events/run-events.ts
```

또는 기존 파이프라인 폴더 안에 둔다.

```text
scripts/pipeline/events/01-collect-events.ts
scripts/pipeline/events/02-analyze-events.ts
scripts/pipeline/events/run-events.ts
```

현재 프로젝트 구조상 `scripts/pipeline` 아래에 두는 것이 관리하기 쉽다.

추천:

```text
scripts/pipeline/events/01-collect-events.ts
scripts/pipeline/events/02-analyze-events.ts
scripts/pipeline/events/run-events.ts
```

package.json 스크립트:

```json
"events": "tsx scripts/pipeline/events/run-events.ts",
"events:collect": "tsx scripts/pipeline/events/01-collect-events.ts",
"events:analyze": "tsx scripts/pipeline/events/02-analyze-events.ts"
```

## 이벤트 수집 1: 네이버 뉴스 기반 후보

기존 `lib/naver.ts`의 `searchNews()`를 재사용한다.

이벤트 탐색 쿼리 예:

```ts
const EVENT_NEWS_QUERIES = [
  "K팝 콘서트 다음주",
  "BTS 공연 예정",
  "블랙핑크 콘서트 예정",
  "아이돌 콘서트 서울",
  "팬미팅 개최",
  "영화제 개막",
  "전시 개막",
  "미술관 특별전",
  "박물관 특별전",
  "팝업스토어 오픈",
  "e스포츠 결승",
  "LCK 결승",
  "문화행사 개최",
  "축제 개막",
];
```

뉴스 기사에서 이벤트 후보를 추출한다.

추출 기준:

- 제목/설명에 아래 키워드가 있어야 한다.

```text
공연, 콘서트, 팬미팅, 투어, 전시, 특별전, 영화제, 축제, 개막, 개최, 오픈, 팝업, 결승, 행사
```

- 날짜 표현이 있어야 한다.

```text
YYYY년 M월 D일
M월 D일
오는 D일
내일
이번 주말
다음 주
```

처음부터 완벽한 자연어 날짜 파싱을 목표로 하지 않는다. 1차는 아래 정도만 처리한다.

- `6월 8일`
- `6월 8일부터`
- `2026년 6월 8일`
- `오는 8일`
- `내일`
- `이번 주말`

날짜를 확정할 수 없는 뉴스는 후보로 남기되, `/events` 최종 표시에서는 제외하거나 낮은 점수로 둔다.

## 이벤트 수집 2: 문체부 지역축제 페이지

대상 URL:

```text
https://www.mcst.go.kr/site/s_culture/festival/festivalList.jsp
```

확인된 특징:

- 지역축제 목록/달력 페이지가 있다.
- 검색기간에 `일주일`, `1개월`, `초기화` 옵션이 있다.
- 지역 필터가 있다.
- 축제명, 기간, 장소, 문의 정보가 노출된다.
- 페이지 안내에 따르면 축제 정보는 한국관광공사 대한민국 구석구석 정보가 연동되어 게시된다.

구현 방식:

1. 먼저 브라우저 개발자 도구 또는 HTML form을 확인해 `일주일` 검색 버튼의 실제 요청 파라미터를 찾는다.
2. 파라미터를 찾으면 fetch로 해당 HTML을 가져온다.
3. HTML에서 축제 카드/목록을 파싱한다.
4. 축제명, 기간, 지역, 장소, 상세 링크, 문의처를 추출한다.
5. 향후 7일 범위와 겹치는 축제만 남긴다.

주의:

- 이 페이지는 API가 아니라 HTML 페이지다.
- HTML 구조 변경에 취약하므로 파서는 보수적으로 만든다.
- 파싱 실패 시 전체 파이프라인이 죽지 않게 한다.
- 문체부 페이지는 보조 소스이므로 실패해도 네이버 뉴스 기반 이벤트는 계속 동작해야 한다.

## 날짜 필터

기준:

```ts
range.from = 오늘
range.to = 오늘 + 7일
```

포함 조건:

```text
event.startDate <= range.to
AND
(event.endDate 없거나 event.endDate >= range.from)
```

즉, 이미 시작했지만 다음 7일 안에도 계속 열리는 전시/축제는 포함한다.

## 중복 병합

동일 이벤트가 뉴스와 문체부 양쪽에 잡힐 수 있다.

병합 기준:

- 제목 정규화 후 유사도
- 날짜 겹침
- 지역/장소 유사성

간단한 1차 기준:

```text
정규화 제목의 핵심 토큰 2개 이상 공통
AND startDate가 같거나 기간이 겹침
```

병합 시:

- `sources` 배열에 모든 출처를 보존한다.
- 공식 출처가 있으면 `officialUrl`로 우선 사용한다.
- 뉴스 출처는 관심도 산정에 사용한다.

## 점수 모델

각 이벤트는 0~100 점수로 평가한다.

추천 필드:

```ts
scores: {
  importance: number;
  interest: number;
  urgency: number;
  scale: number;
  reliability: number;
}
```

종합 점수:

```ts
score =
  importance * 0.30 +
  interest * 0.25 +
  urgency * 0.20 +
  scale * 0.15 +
  reliability * 0.10
```

### importance

문화적으로 중요한 정도.

상승 조건:

- BTS, 블랙핑크, 뉴진스, 세븐틴 등 대형 아티스트
- 국립중앙박물관, 예술의전당, 세종문화회관 등 주요 기관
- 국제 영화제/비엔날레/아트페어
- 지역 대표 축제
- 최초/개막/특별전/월드투어/결승 등 키워드

### interest

사람들이 관심을 가질 가능성.

상승 조건:

- 네이버 뉴스 언급량
- 서로 다른 언론사 수
- 최근 기사 여부
- 팬덤성 키워드
- 매진, 추가 공연, 화제, 돌풍, 예매 등 키워드

### urgency

임박성.

상승 조건:

- 오늘/내일/이번 주말 시작
- 향후 7일 안에 종료
- 단기 행사

### scale

규모.

상승 조건:

- 전국/글로벌 투어
- 대형 공연장/아레나/스타디움
- 관객 수, 회차 수, 도시 수, 국가 수
- 대형 지역축제

### reliability

출처 신뢰도.

상승 조건:

- 문체부/공식기관 출처
- 공식 행사 페이지
- 주요 언론 다수 보도
- 장소/기간/문의처가 명확함

## 카테고리 분류

자동 분류 기준:

```text
concert: 콘서트, 공연, 투어, 팬미팅, 아이돌, K팝
festival: 축제, 페스티벌, 야행, 문화제
exhibition: 전시, 특별전, 미술관, 박물관, 비엔날레
performance: 뮤지컬, 연극, 무용, 클래식, 국악
film: 영화제, 상영회, 개봉 행사
esports: e스포츠, LCK, 결승, MSI
popup: 팝업, 팝업스토어, 브랜드 행사
heritage: 문화재, 전통문화, 무형유산, 한복, 궁궐
other: 기타
```

## LLM 사용 여부

1차 구현에서는 LLM 사용을 최소화한다.

추천:

- 수집/날짜 파싱/점수화는 코드로 처리
- 최종 `/events` 문구 정리와 설명문만 LLM을 선택적으로 사용

하지만 운영 안정성을 위해 1차는 LLM 없이도 동작해야 한다.

즉:

```text
events pipeline은 LLM 없이 기본 생성 가능해야 한다.
```

## 페이지 구현

새 페이지:

```text
app/events/page.tsx
```

데이터 로더:

```text
lib/events-content.ts
```

컴포넌트:

```text
components/EventCard.tsx
components/EventsView.tsx
```

페이지 구성:

- 상단 요약
  - `앞으로 7일, 놓치면 아쉬운 문화 이벤트`
  - 기간 표시
  - 총 후보 수 / 선정 수

- 이벤트 카드
  - 이벤트명
  - 날짜
  - 지역/장소
  - 카테고리
  - 점수 근거
  - 출처 링크

- 필터
  - 지역
  - 카테고리
  - 날짜

1차에서는 필터를 서버 렌더링 고정 UI로 두거나, 간단한 클라이언트 상태로 구현한다.

## 화면 문구

점수는 과장하지 않는다.

추천 문구:

```text
수집 뉴스와 공식 축제 정보를 바탕으로 산정한 우선순위입니다.
일정은 주최 측 사정에 따라 변경될 수 있으니 방문 전 공식 링크를 확인하세요.
```

각 카드 근거 예:

```text
선정 근거: 8개 기사 · 5개 매체 · 공식 축제 정보 확인 · 이번 주말 개막
```

## 기존 뉴스 파이프라인과의 관계

이벤트 파이프라인은 뉴스 issue 생성과 분리한다.

이유:

- 뉴스 매거진은 지난 1주일 회고다.
- 이벤트 페이지는 향후 7일 예고다.
- 둘을 한 파이프라인에 강하게 묶으면 발행 실패 지점이 늘어난다.

추천 운영:

```text
npm run pipeline        # 기존 뉴스 issue 생성
npm run events          # 향후 7일 이벤트 생성
```

나중에 안정화되면 `make.sh` 안에서 둘 다 실행하도록 확장할 수 있다.

1차에서는 `make.sh`, `publish.sh`를 건드리지 않아도 된다. `/events` 데이터 생성은 별도 명령으로 둔다.

## 문체부 페이지 파싱 주의사항

문체부 페이지에는 "일주일" 검색 UI가 있지만 실제 구현 시에는 반드시 요청 파라미터를 확인해야 한다.

작업 순서:

1. `festivalList.jsp` HTML form 확인
2. `일주일` 버튼 클릭 시 요청되는 query/form parameter 확인
3. 해당 parameter로 fetch 테스트
4. 목록 HTML에서 반복되는 축제 아이템 selector 확인
5. 파싱 실패 시 빈 배열 반환

절대 하면 안 되는 것:

- 페이지 구조를 보지 않고 정규식만으로 무작정 파싱
- 파싱 실패 시 전체 빌드 실패
- 날짜가 불명확한 행사를 확정 일정처럼 표시

## 검증 기준

필수:

```bash
npm run events
npx tsc --noEmit
npx next build
```

수동 검수:

- `/events` 페이지가 빌드되는가
- 향후 7일 범위 밖 이벤트가 섞이지 않는가
- BTS 공연 같은 대형 뉴스 기반 이벤트가 지역축제보다 아래로 밀리지 않는가
- 문체부 지역축제가 보조 후보로 들어오는가
- 날짜/장소/출처 링크가 없는 카드는 표시 품질이 떨어지지 않는가
- 공식 링크 확인 안내 문구가 보이는가

## 구현 난이도

1차 구현 난이도는 **중상**이다.

이유:

- 문체부 HTML 파싱이 필요하다.
- 뉴스 기사에서 이벤트 날짜를 추출해야 한다.
- 같은 이벤트 중복 병합이 필요하다.
- 이벤트는 날짜 오류가 사용자 피해로 이어질 수 있다.

하지만 기존 프로젝트에 이미 있는 자산을 활용할 수 있다.

- `lib/naver.ts`
- `lib/paths.ts`
- `scripts/pipeline` 구조
- 중요도 분석 로직 경험
- Next.js 정적 페이지 구조

따라서 범위를 "향후 7일 문화 이벤트 큐레이션"으로 제한하면 구현 가능하다.

## 1차 구현 순서

1. `types/event.ts` 추가
2. `lib/events-content.ts` 추가
3. `scripts/pipeline/events/01-collect-events.ts` 추가
4. 네이버 뉴스 기반 이벤트 후보 추출 구현
5. 문체부 지역축제 HTML 파서 구현
6. `scripts/pipeline/events/02-analyze-events.ts` 추가
7. 중복 병합과 점수화 구현
8. `content/events/{slug}/events.json` 생성
9. `app/events/page.tsx` 추가
10. `components/EventsView.tsx`, `components/EventCard.tsx` 추가
11. `package.json`에 `events`, `events:collect`, `events:analyze` 추가
12. `npx tsc --noEmit`, `npx next build` 검증

## 최종 요약

이 기능은 해야 한다. 다만 "지역축제 페이지"가 아니라 "향후 7일 문화 이벤트 큐레이션"으로 만들어야 한다.

문체부 지역축제는 좋은 공식 보조 소스다. 그러나 BTS 공연 같은 대형 이벤트를 잡으려면 네이버 뉴스 기반 이벤트 추출이 메인이 되어야 한다.

최종 구조:

```text
네이버 뉴스 이벤트 후보
+ 문체부 지역축제 후보
-> 중복 병합
-> 향후 7일 필터
-> 중요도/관심도/임박성 점수화
-> /events 페이지
```

이렇게 구현하면 현재 프로젝트는 과거 뉴스 큐레이션뿐 아니라, 앞으로 열릴 문화 이벤트를 미리 알려주는 서비스로 확장된다.
