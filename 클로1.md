# 클로1 — 지피1.md(향후 7일 문화 이벤트 페이지) 초정밀 검토

> 작성: Claude. 실제 코드(`lib/naver.ts`·`types/pipeline.ts`·`news_raw` 크론·`SiteHeader`·발행 경로)를 직접 대조해 검증함.

---

## 1. 판정 (Verdict)

**기능 자체는 추진 가치가 크다(과거 회고 + 미래 예고 = 서비스 완성형). 그러나 "이대로" 패치는 비추천.**
아래 🔴 구조 수정 2건을 반영한 뒤 구현해야 한다. 이 2건은 취향 문제가 아니라 **합법성·안정성·데이터 현실**의 문제다.

---

## 2. 🔴 구조 수정 1 — 문체부 HTML 크롤링 → **한국관광공사 공식 API로 대체**

지피1은 `mcst.go.kr/.../festivalList.jsp` HTML을 파싱하자고 한다. **이건 틀린 선택이다. 지피1 스스로 단서를 적어놨다**: *"하단 축제정보는 한국관광공사 대한민국 구석구석 정보가 연동된 것"* — 즉 **그 페이지의 데이터 원천(한국관광공사)이 공공데이터포털(data.go.kr)에 공식 무료 OPEN API(TourAPI '축제공연행사 조회')를 제공한다.**

| 비교 | 문체부 HTML 크롤링 (지피1) | 관광공사 공식 API (수정안) |
|------|---------------------------|---------------------------|
| 합법성/약관 | 회색지대(크롤링) | ✅ 공식 제공(공공누리, 출처표기) |
| 안정성 | JSP form/HTML 구조 변경에 취약(지피1도 인정) | ✅ 버전 관리되는 JSON API |
| 날짜/장소 | HTML에서 긁어 파싱 | ✅ **시작일·종료일·장소·지역 구조화 필드** |
| 기간 검색 | "일주일" 버튼 파라미터 리버싱 필요 | ✅ 날짜 파라미터로 기간 조회 |
| 이미지 | 없음/불명 | ✅ 행사 이미지 URL 제공(출처표기로 합법 사용 — 보너스) |
| 비용 | 무료 | ✅ 무료(키 발급만, 일일 한도 충분) |
| 구현량 | HTML 파서 + 실패 방어 | ✅ fetch + JSON (파서 불필요) |

→ **크롤링 코드를 한 줄도 쓰지 말 것.** 공공데이터포털에서 "한국관광공사 축제공연행사(TourAPI)" 키 발급 → `.env.local`에 추가(기존 NAVER/PEXELS 관행, 커밋 금지) → `lib/tourapi.ts` 클라이언트. 프로젝트의 "100% 무료·합법" 원칙에 정확히 부합하고, 지피1의 "파싱 실패 방어" 걱정 자체가 사라진다. 난이도도 중상→**중**으로 내려간다.

## 3. 🔴 구조 수정 2 — "뉴스가 메인"은 데이터 현실과 충돌. **역할 분담으로 변경**

코드로 확인한 사실: Naver 검색 API의 `NewsItem`은 **title + description(요약 스니펫)뿐, 기사 본문이 없다**(`types/pipeline.ts` 4-14행, 기존 파이프라인의 알려진 제약). 이벤트 날짜는 본문 깊숙이 있는 경우가 많아 **뉴스에서 날짜 확보율이 구조적으로 낮다.** "뉴스 기반이 메인"(지피1)으로 잡으면 날짜 없는 후보가 대량 발생한다.

추가 함정(지피1 미명시): "오는 8일", "내일", "이번 주말"은 **기사 발행일(pubDate) 기준 상대 표현**이다. 파이프라인 실행일 기준으로 해석하면 전부 틀린다. 연도 없는 "6월 8일"도 pubDate 연도로 보정(지났으면 +1년)해야 한다. **이벤트 날짜 오류 = 사용자 실피해**(헛걸음)라 가장 보수적으로 다뤄야 할 부분.

→ **수정안: 역할 분담**
- **일정(날짜·장소)의 근간 = 관광공사 API** (구조화 날짜라 오류 없음)
- **뉴스 = ① 관심도·중요도 가중 신호**(언급 기사 수·매체 수 — 기존 02-analyze 방식 그대로) **② API에 없는 대형 이벤트(콘서트·팝업·e스포츠 등) 보완 발굴**
- 뉴스-단독 이벤트는 1차에서 **명시적 절대 날짜("2026년 6월 8일", "6월 8일")가 제목/스니펫에 있는 것만 채택**. 상대 표현 파싱("오는 8일"·"내일"·"이번 주말")은 pubDate-기준 해석 복잡성 때문에 **2차로 연기**.
- `CultureEvent`에 **`dateConfidence: "exact" | "approx"`** 필드 추가 — approx는 카드에 "일정 확인 필요" 표기.

## 4. 🟡 보완 사항 (지피1의 구멍)

1. **발행 경로 미정의** — events.json을 만들고 끝이 아니다. 이 사이트는 **git push = 발행** 구조이고, `publishToGit`은 일부러 `content/issues/{slug}`만 add 하도록 좁혀놨다(테스트호 오발행 방지 안전장치). 즉 `content/events/{slug}/`는 **누가 언제 커밋·푸시하나가 빠져 있다.** → 1차: `publish-events.sh`(또는 publish.sh에 옵션) — `git add content/events/{slug}` 슬러그 한정 커밋·푸시로 동일 안전장치 적용.
2. **진입점 누락** — `/events` 페이지를 만들어도 가는 길이 없다. `SiteHeader` 내비는 현재 홈/구독뿐(확인됨) → "이번 주 이벤트" 링크 추가 필요.
3. **기존 누적 풀 재활용 누락** — `news_raw`(Supabase, Vercel 크론으로 **이미 매일 수집 중**)에 최근 7~14일 기사가 쌓여 있다. 이벤트 후보 추출은 이 풀을 1차 소스로 쓰고, 신규 쿼리는 소수만 추가하면 API 호출·중복 로직이 준다.
4. **쿼리 설계** — "BTS 공연 예정"식 아티스트 고정 쿼리는 유지보수가 나쁘고 검색 효율도 낮다. 일반 쿼리("내한공연", "티켓 오픈", "콘서트 개최", "전시 개막", "팝업스토어 오픈", "결승" 등) 위주로.
5. **1차 필터 UI 생략(YAGNI)** — 지역/카테고리/날짜 필터는 2차로. 1차는 점수순 정렬 + 카테고리 뱃지 + 기간 표기로 충분.
6. 점수 모델(0–100, 가중합, reliability 포함)·타입 설계·`content/events/{slug}` 정적 패턴·**LLM 없이 동작**·뉴스 파이프라인과 분리 — **모두 동의.** 기존 02-analyze 경험과 일관된다. 가중치는 "v1 임시값" 주석 관행 유지.
7. 중복 병합(공식API↔뉴스)은 기존 클러스터링과 같은 보수 원칙: 핵심 토큰 2개+기간 겹침 — 동의. 병합 시 `officialUrl` 우선·뉴스는 관심도 산정 — 동의.
8. 화면의 "일정은 변경될 수 있으니 공식 링크 확인" 문구 — 동의(필수).

## 5. 수정된 1차 구현 순서 (지피1 대체)

1. 공공데이터포털 키 발급(사용자 작업, 무료) → `.env.local`에 `TOUR_API_KEY`
2. `types/event.ts` (+`dateConfidence`)
3. `lib/tourapi.ts` — 관광공사 축제공연행사 조회(기간 파라미터, JSON)
4. `scripts/pipeline/events/01-collect-events.ts` — ① TourAPI 7일 조회 ② news_raw+신규 일반쿼리에서 절대날짜 이벤트 후보 추출
5. `scripts/pipeline/events/02-analyze-events.ts` — 병합·점수화·랭킹 (LLM 없음, deterministic)
6. `run-events.ts` → `content/events/{slug}/events.json`
7. `lib/events-content.ts` + `app/events/page.tsx` + `EventsView/EventCard` + **SiteHeader 링크**
8. `publish-events.sh`(슬러그 한정 git add) 또는 publish.sh 옵션
9. `npx tsc --noEmit` · `npx next build` · 실데이터 1회 생성 후 7일 범위·날짜 정확성 육안 검수

## 6. 난이도 재평가

지피1 "중상" → 수정안 적용 시 **중**. (HTML 파서·"일주일" 파라미터 리버싱·상대날짜 파싱이라는 3대 리스크가 모두 제거되고, 남는 건 익숙한 패턴 — API 클라이언트·점수화·정적 페이지.)

## 7. 한 줄 결론

**기능은 GO. 단 ① 문체부 크롤링을 원천 데이터의 공식 API(한국관광공사 TourAPI)로 교체하고, ② "뉴스=발굴·관심도 / 공식API=날짜·장소"로 역할을 분담하며, ③ 발행 경로(content/events 슬러그 한정 커밋)와 헤더 진입점을 추가한 수정안으로 구현하라.** 이 세 가지 없이 "이대로" 패치하면 크롤링 취약성과 날짜 오류가 사용자에게 그대로 노출된다.
