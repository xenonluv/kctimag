# 02-analyze.ts 문화 관련성 필터 수정방안

## 결론

리뷰 지적은 타당하다. 현재 `scripts/pipeline/02-analyze.ts`의 `isRelevantCultureItem()`은 정상적인 문화 뉴스를 분석 후보에서 누락시킬 수 있다.

문제 핵심:

```ts
function isRelevantCultureItem(item: NewsItem, text: string): boolean {
  if (hasAny(text, NON_CULTURE_TERMS)) return false;
  const title = normalizeText(item.title);
  const terms = CATEGORY_TERMS[item.category] ?? [];
  if (terms.length === 0) return true;
  return hasAny(title, terms) || hasAny(title, MAJOR_TERMS);
}
```

비문화 판정은 `title + description` 기반 `text`를 보면서, 문화 관련성 통과는 `title`만 본다. 네이버 검색 API는 검색어가 제목이 아니라 description에만 매칭된 기사도 반환할 수 있으므로, 제목에 카테고리 용어가 없고 description에만 있는 정상 기사가 누락된다.

예:

```text
title: 뉴진스, 7월 신보로 돌아온다
description: 아이돌 컴백 일정과 K팝 시장 반응...
category: kpop
```

현재 로직에서는 제목에 `K팝`, `아이돌`, `앨범`, `콘서트` 등이 없으면 탈락할 수 있다. 분석 결과가 하나라도 있으면 `curate()`가 분석 후보 중심으로 입력을 구성하므로, 이 기사는 큐레이션 LLM에도 올라가지 못할 수 있다.

## 수정 원칙

- 문화 관련성 판정은 제목만 보지 않는다.
- `title + description + categoryLabel` 전체 텍스트를 사용한다.
- 수집 단계에서 이미 카테고리 쿼리를 통해 들어온 기사라는 점을 신뢰하되, 비문화/저가치 필터는 유지한다.
- `shouldExclude(title)`은 기존처럼 유지한다.
- `NON_CULTURE_TERMS`는 제목만이 아니라 전체 텍스트에서 계속 검사한다.
- 제목에 카테고리 용어가 없어도 description 또는 categoryLabel에 근거가 있으면 통과시킨다.

## 최종 수정안

### 1. 후보 텍스트 구성 확인

`analyze()`에서 후보를 만들 때 `text`는 반드시 아래처럼 제목과 설명을 모두 포함해야 한다.

```ts
const text = normalizeText(`${item.title} ${item.description} ${item.categoryLabel}`);
```

이미 비슷하게 구현되어 있다면 그대로 유지하되, `categoryLabel`이 빠져 있으면 추가한다.

### 2. `isRelevantCultureItem()` 수정

현재:

```ts
const title = normalizeText(item.title);
return hasAny(title, terms) || hasAny(title, MAJOR_TERMS);
```

변경:

```ts
function isRelevantCultureItem(item: NewsItem, text: string): boolean {
  if (hasAny(text, NON_CULTURE_TERMS)) return false;

  const terms = CATEGORY_TERMS[item.category] ?? [];
  if (terms.length === 0) return true;

  const categoryLabel = normalizeText(item.categoryLabel);
  const relevanceText = normalizeText(`${item.title} ${item.description} ${categoryLabel}`);

  return (
    hasAny(relevanceText, terms) ||
    hasAny(relevanceText, MAJOR_TERMS) ||
    hasAny(categoryLabel, terms)
  );
}
```

`hasAny(categoryLabel, terms)`는 보조 안전장치다. 카테고리 라벨이 `K-pop·음악`처럼 저장되어 있고, 카테고리 term에 `음악`이 있으면 정상 통과할 수 있다.

### 3. 더 안전한 대안

위 방식보다 더 단순하게 가려면 아래도 가능하다.

```ts
function isRelevantCultureItem(item: NewsItem, text: string): boolean {
  if (hasAny(text, NON_CULTURE_TERMS)) return false;

  const terms = CATEGORY_TERMS[item.category] ?? [];
  if (terms.length === 0) return true;

  return hasAny(text, terms) || hasAny(text, MAJOR_TERMS);
}
```

다만 이 경우 `text`에 `categoryLabel`이 반드시 포함되어야 한다. 구현 실수를 줄이려면 2번 최종 수정안을 권장한다.

## 추가 보완

`CATEGORY_TERMS.kpop`에는 현재 `신보`, `컴백`, 주요 그룹명이 없다. 제목이 아티스트명 중심인 K-pop 기사 누락을 줄이려면 아래 term을 추가한다.

```ts
kpop: [
  "K팝",
  "케이팝",
  "아이돌",
  "가수",
  "음악",
  "앨범",
  "신보",
  "컴백",
  "콘서트",
  "투어",
  "차트",
  "뉴진스",
  "아이브",
  "르세라핌",
  "세븐틴",
  "스트레이 키즈",
]
```

단, 주요 그룹명 추가는 과하게 늘리지 않는다. 핵심은 제목만 보던 필터를 전체 텍스트 기반으로 바꾸는 것이다.

## 테스트 케이스

가능하면 `02-analyze.ts` 내부 함수가 export되지 않더라도, `analyze()` 입력으로 아래 raw fixture를 만들어 수동 또는 간단 테스트를 돌린다.

### 통과해야 하는 케이스

```ts
{
  title: "뉴진스, 7월 신보로 돌아온다",
  description: "아이돌 컴백 일정과 K팝 시장 반응이 주목된다.",
  category: "kpop",
  categoryLabel: "K-pop·음악"
}
```

기대:

- 분석 후보에 포함
- `analysis.issues`에 포함

### 계속 제외되어야 하는 케이스

```ts
{
  title: "대통령실, 문화 예산 관련 입장 발표",
  description: "정부와 국회가 예산안을 논의했다.",
  category: "kpop",
  categoryLabel: "K-pop·음악"
}
```

기대:

- `NON_CULTURE_TERMS` 때문에 제외

### 저가치 기사는 계속 제외

```ts
{
  title: "[포토] 아이돌 A, 공항패션 눈길",
  description: "K팝 스타의 출국길 사진",
  category: "kpop",
  categoryLabel: "K-pop·음악"
}
```

기대:

- `shouldExclude(title)` 때문에 제외

## 검증 명령

수정 후 반드시 실행한다.

```bash
npx tsc --noEmit
npx next build
```

가능하면 mock 파이프라인도 확인한다.

```bash
LLM_MODE=mock npm run pipeline
```

## 코드리뷰 기준

패치 후 아래 조건을 모두 만족해야 한다.

- `isRelevantCultureItem()`이 제목만으로 문화 관련성을 판정하지 않는다.
- description에만 카테고리 관련어가 있는 정상 문화 뉴스가 분석 후보에 남는다.
- `NON_CULTURE_TERMS` 필터는 전체 텍스트 기준으로 유지된다.
- `shouldExclude(title)` 기반 저가치 기사 제외는 유지된다.
- 분석 후보가 지나치게 넓어지는 것을 막기 위해 category terms 또는 major terms 확인은 계속 유지된다.

## 최종 합의

리뷰 코멘트는 수정해야 한다. 현재 방식 그대로 두면 정상 기사가 분석 후보에서 누락되고, 이후 큐레이션 LLM에도 전달되지 않을 수 있다.

따라서 최종 수정은 다음 한 줄로 요약된다.

```text
문화 관련성 판정은 title이 아니라 title + description + categoryLabel 기반으로 수행한다.
```
