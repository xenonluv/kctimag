# 뉴스 우선순위 분석 최종 합의안

## 최종 결론

이 안은 패치해도 되는 최종 합의안이다. 단, 아래 제약을 그대로 지켜야 한다.

```text
collect -> analyze(로컬 코드) -> curate(LLM 1회) -> images -> issue.json
```

핵심 합의:

- 외부 API, 크롤링, 형태소 분석기, 임베딩 API를 추가하지 않는다.
- `scripts/pipeline/02-analyze.ts`는 LLM을 호출하지 않는 순수 코드 분석 단계로 만든다.
- LLM 호출은 기존 `scripts/pipeline/02-curate.ts`의 큐레이션 1회만 유지한다.
- 분석은 `CURATE_PER_CAT`, `CURATE_MAX_INPUT`으로 자르기 전 `raw.items` 전체에 대해 수행한다.
- 보도량은 원시 기사 수보다 서로 다른 언론사 수를 더 강한 신호로 본다.
- 심리강도는 클릭베이트를 올릴 수 있으므로 낮은 가중치로만 쓴다.
- 점수는 실제 검색량/SNS 반응이 아니라 "수집 뉴스 기반 우선순위 추정치"다.
- 기존 발행 JSON과 화면 렌더링이 깨지지 않도록 모든 새 필드는 optional로 추가한다.

## 구현 파일

수정 또는 추가 대상:

- `types/pipeline.ts`
- `types/issue.ts`
- `lib/schemas.ts`
- `scripts/pipeline/02-analyze.ts`
- `scripts/pipeline/02-curate.ts`
- `scripts/pipeline/run.ts`
- `package.json`
- 선택 UI 표시 시 `components/IssueView.tsx`, `app/page.tsx`

## 타입 합의

### `types/pipeline.ts`

기존 `AnalyzedIssue.intensities`는 더 이상 사용하지 않는다. 새 코드에서는 `scores`만 사용한다.

최종 타입:

```ts
export interface IssueScores {
  importance: number;
  impact: number;
  interest: number;
  psychological: number;
  realtime: number;
}

export interface AnalyzedIssue {
  rank: number;
  heading: string;
  category: string;
  summary: string;
  scores: IssueScores;
  score: number;
  rationale: string;
  sourceIndexes: number[];
  sourceCount: number;
  outletCount: number;
  outlets: string[];
  primaryIndex: number;
}

export interface Analysis {
  analyzedAt: string;
  method: "local-heuristic-v1";
  note: string;
  issues: AnalyzedIssue[];
}
```

규칙:

- `sourceIndexes`는 반드시 `raw.items` 원본 배열의 인덱스다.
- `primaryIndex`도 반드시 `raw.items` 원본 배열의 대표 기사 인덱스다.
- `score`와 모든 `scores.*`는 0~100 범위로 clamp한다.

### `types/issue.ts`

`IssueScores`는 반드시 type-only import로 가져온다. 중복 타입을 만들지 않는다.

파일 상단에 추가:

```ts
import type { IssueScores } from "@/types/pipeline";
```

추가 타입:

```ts
export interface NewsPriorityAnalysis {
  rank: number;
  score: number;
  scores: IssueScores;
  rationale: string;
  sourceCount: number;
  outletCount: number;
}
```

기존 인터페이스에는 optional 필드만 추가한다.

```ts
export interface NewsEntry {
  // 기존 필드 유지
  analysis?: NewsPriorityAnalysis;
}

export interface EditorPick {
  // 기존 필드 유지
  analysis?: NewsPriorityAnalysis;
}

export interface IssueMeta {
  // 기존 필드 유지
  analysis?: {
    method: "local-heuristic-v1";
    note: string;
    issueCount: number;
  };
}
```

중요:

- `import type`을 반드시 사용한다.
- `types/pipeline.ts`가 `types/issue.ts`를 import하지 않도록 한다.
- 런타임 순환 의존이 생기면 안 된다.

## 분석 단계

### 새 파일

```text
scripts/pipeline/02-analyze.ts
```

최종 시그니처:

```ts
export function analyze(raw: RawNews): Analysis
```

CLI 직접 실행도 지원한다.

```ts
const slug = process.argv[2] || new Date().toISOString().slice(0, 10);
const raw = readJson<RawNews>(path.join(tmpDir(slug), "raw-news.json"));
writeJson(path.join(tmpDir(slug), "analysis.json"), analyze(raw));
```

### 분석 순서

```text
raw.items 전체
-> 링크 기준 중복 제거
-> 보수적 이슈 클러스터링
-> 이슈별 sourceCount/outletCount 계산
-> importance/impact/interest/psychological/realtime 계산
-> 종합 score 계산
-> score 내림차순 정렬
-> rank 부여
```

### 클러스터링 규칙

외부 패키지를 추가하지 않는다. 문자열 휴리스틱만 사용한다.

권장 규칙:

- 제목과 설명을 합쳐 정규화한다.
- HTML, 특수문자, 따옴표, 괄호, 과도한 공백을 제거한다.
- 2글자 이하 토큰과 stopword를 제거한다.
- 같은 카테고리 안에서만 기본적으로 묶는다.
- 공통 핵심 토큰 2개 이상이면서 Jaccard 유사도 0.35 이상이면 같은 이슈로 본다.
- `BTS`, `블랙핑크`, `넷플릭스`, `올리브영` 같은 대형 키워드 하나만 같다고 묶지 않는다.

클러스터링은 완벽하지 않다는 전제로 구현한다. 보도량은 최종 결정이 아니라 참고 신호다.

### 점수 모델

최종 가중치:

```ts
score =
  scores.importance * 0.32 +
  scores.impact * 0.27 +
  scores.interest * 0.23 +
  scores.psychological * 0.08 +
  scores.realtime * 0.10;
```

코드 주석에 `v1 heuristic weights`라고 명시한다. 이 가중치는 첫 실주행 후 조정 가능하다.

세부 기준:

- `importance`: 기록, 수상, 차트, 흥행, 출시, 투자, 인수, 계약, 선판매, 산업 변화, 주요 인물/작품/기업이면 상승
- `impact`: 글로벌, 해외, 북미, 일본, 중국, 수출, 시장, 플랫폼, 팬덤, 관객 수, 매출, 1위, 100만, 국가/권역이면 상승
- `interest`: `outletCount`를 가장 크게 반영하고 `sourceCount`는 보조로 반영
- `psychological`: 논란, 반발, 열풍, 품절, 기대, 최초, 돌풍, 위기 등은 소폭 상승
- `realtime`: 최근 24~48시간 집중도와 한 주 내 지속 보도 여부를 낮은 가중치로 반영

홍보성/선정성/저가치 신호:

- `shouldExclude`를 `lib/news-filter.ts`에서 재사용한다.
- `shouldExclude(title)`이 true인 기사는 분석 입력에서 제외한다.
- `shouldExclude`에는 걸리지 않지만 동일 제목/표현이 반복되는 보도자료성 이슈는 제외하지 않고 `interest` 상승폭만 제한한다.
- 동일 매체 반복 기사와 보도자료성 제목 반복은 `interest` 상승폭을 제한한다.

## 큐레이션 단계

### `scripts/pipeline/02-curate.ts`

함수 시그니처를 확장한다.

```ts
export async function curate(
  raw: RawNews,
  analysis?: Analysis,
): Promise<CuratedDoc>
```

분석 결과가 있으면 LLM 입력은 분석 상위 이슈 중심으로 구성한다.

입력 상한:

- 상위 이슈 전체 최대 80개
- 카테고리별 최소 8개 이슈 보장
- 각 이슈당 대표 기사 최대 3개

기존 환경변수 호환:

- `CURATE_MAX_INPUT`은 LLM에 넣을 대표 기사 총량 상한으로 유지한다.
- `CURATE_PER_CAT`은 카테고리별 분석 이슈 또는 대표 기사 상한으로 재해석한다.
- 환경변수 이름은 바꾸지 않는다.

프롬프트에 반드시 포함:

```text
아래 점수는 외부 검색량/SNS 반응이 아니라 수집 뉴스 풀 기반 추정치다.
원시 기사 수보다 서로 다른 언론사 수와 문화적 의미를 더 신뢰하라.
홍보성 보도자료, 선정적 클릭베이트, 단순 근황은 점수가 높아도 제외하라.
```

### 인덱스 매핑 규칙

이 부분은 오류 가능성이 높으므로 반드시 아래 규칙을 따른다.

- LLM에게 보여주는 후보 배열을 `candidateItems`로 따로 만든다.
- `CurateEntry.index`는 기존처럼 `candidateItems`의 0부터 시작하는 인덱스다.
- `raw.items` 원본 인덱스를 직접 `CurateEntry.index`로 쓰지 않는다.
- 후보 생성 시 `candidateIndex -> rawIndex -> issueRank` 매핑을 만든다.
- 조립 단계에서는 `CurateEntry.index`를 `rawIndex`로 변환한 뒤 반드시 `toEntry(raw.items, rawIndex, ...)` 형태로 호출한다.
- `toEntry(candidateItems, rawIndex, ...)`처럼 후보 배열과 원본 인덱스를 섞어 쓰면 안 된다.
- `issueRank`가 있으면 해당 rank의 분석 점수를 붙인다.
- `issueRank`가 없으면 `rawIndex`가 포함된 `sourceIndexes`를 찾아 분석 점수를 붙인다.

### `lib/schemas.ts`

`issueRank`는 optional로 추가한다.

```ts
export const CurateEntrySchema = z.object({
  index: z.number().int(),
  issueRank: z.number().int().optional(),
  blurb: z.string(),
  imagePrompt: z.string(),
  imageQuery: z.string().optional(),
});
```

기존 mock도 `issueRank` 없이 통과해야 한다.

## `run.ts` 변경

`scripts/pipeline/run.ts`에 analyze 단계를 추가한다.

```ts
import { analyze } from "./02-analyze";

const analysis = analyze(raw);
writeJson(`${tmpDir(slug)}/analysis.json`, analysis);

const curated = await curate(raw, analysis);
```

`issue.meta.analysis`에는 전체 분석 메타만 저장한다.

```ts
analysis: {
  method: analysis.method,
  note: analysis.note,
  issueCount: analysis.issues.length,
}
```

전체 `analysis.issues`를 `content/issues/{slug}/issue.json`에 저장하지 않는다.

## `issue.json` 저장 범위

저장한다:

- `meta.analysis`
- 선택된 `NewsEntry.analysis`
- `editorPick.analysis`

저장하지 않는다:

- 전체 `analysis.issues`
- 전체 원문 기사 목록
- 클러스터링 내부 토큰

## UI 반영

1차 구현에서는 작게 표시한다.

권장 문구:

```text
수집 뉴스 기반 우선순위 분석
선정 근거: 12개 기사 · 8개 매체
```

점수를 노출한다면 반드시 "수집 뉴스 기반 추정치"라는 표현을 함께 둔다.

## `package.json`

스크립트를 추가한다.

```json
"pipeline:analyze": "tsx scripts/pipeline/02-analyze.ts"
```

## 검증 명령

필수:

```bash
npx tsc --noEmit
npx next build
```

가능하면 추가:

```bash
LLM_MODE=mock npm run pipeline
```

수동 검수:

- `.pipeline-tmp/{slug}/analysis.json`이 생성되는가
- `analysis.issues`가 `score` 내림차순인가
- `sourceIndexes`와 `primaryIndex`가 `raw.items` 원본 인덱스인가
- 보도자료 반복이 상위권을 장악하지 않는가
- 글로벌/산업 파급 이슈가 상위권에 오는가
- 단순 화보/근황/출연 기사가 다시 살아나지 않는가
- 기존 `content/issues/*/issue.json` 렌더링이 깨지지 않는가

## 비범위

이번 패치에서 하지 않는다.

- Google Trends, Naver DataLab, SNS API
- 댓글 수, 조회 수 크롤링
- 외부 임베딩 API
- 형태소 분석기 패키지 추가
- 분석 단계 LLM 보정 호출
- 대형 점수 대시보드 UI
- 기존 발행본 JSON 마이그레이션

## 코드리뷰 기록

### 1차 리뷰

점검:

- LLM 1회 원칙이 깨지지 않는가
- `analyze`가 순수 코드 함수로 고정됐는가
- 외부 API/크롤링/형태소 분석기 추가가 비범위로 명시됐는가
- 분석이 `raw.items` 전체에서 먼저 수행되는가

결과:

- 문제 없음.

### 2차 리뷰

점검:

- `IssueScores` import가 모호하지 않은가
- `types/issue.ts`에서 반드시 `import type { IssueScores } from "@/types/pipeline";`를 쓰도록 명시됐는가
- `types/pipeline.ts`가 `types/issue.ts`를 import하지 않아 순환 의존이 없는가

결과:

- 문제 없음. type-only import로 고정했다.

### 3차 리뷰

점검:

- `CurateEntry.index`가 후보 배열 인덱스인지 원본 인덱스인지 명확한가
- `candidateIndex -> rawIndex -> issueRank` 매핑이 필수로 명시됐는가
- `sourceIndexes`와 `primaryIndex`가 원본 `raw.items` 기준임이 명확한가

결과:

- 문제 없음.

### 4차 리뷰

점검:

- 전체 `analysis.issues`를 발행 JSON에 넣지 않도록 제한했는가
- 기존 `issue.json`과 화면 렌더링은 optional 필드만 추가해 유지되는가
- mock fallback이 `issueRank` 없이도 통과하는가
- 필수 검증 명령이 포함됐는가

결과:

- 문제 없음. 이 문서 기준으로 패치해도 된다.

### 5차 리뷰

점검:

- `CurateEntry.index`를 `rawIndex`로 변환한 뒤 `toEntry`에 넘기는 방식이 안전한가
- 후보 배열과 원본 인덱스를 섞어 쓰는 오류 가능성을 문서가 차단하는가

결과:

- 문제 없음. `toEntry(raw.items, rawIndex, ...)`로 고정했다.
